Implement these next:

- [DONE] Save system to store gamedata between sessions
    - Character levels
    - Resources
    - World Map State
    - local for now, cloud based later
    - Implemented as SaveManager autoload (JSON to user://savegame.json) + save/load
      orchestration in Base (scripts/base.gd's _save_game/_load_game and helpers).
      Triggered via F5 (quicksave) / F9 (quickload) - no menu UI yet, just a HUD
      flash message (HUD.flash_message). Covers: GameState resources/population,
      every WorldGrid tree (position/maturity/wood remaining - saplings regrow
      from scratch rather than resuming mid-grow), every placed building (fixed
      Outpost Hall/Farm/Woodpile are implicit; player-placed ones are rebuilt from
      catalog id + grid origin), and each of the 3 fixed characters' skill_xp and
      current work assignment. No dedicated tests were added (no test framework
      in this repo); verified via headless boot only, not a full editor run -
      recommend an in-editor playtest (assign a worker, let it tick, F5, change
      state, F9, confirm it reverts) before relying on this.

- [DONE] Realistic travel and labor times for work facilities
    - Facilities need inputs brought to them from the stockpile and output brought out to the stockpile
        - Set up buffers and carry limits to make logistics efficient
    - Overhaul movement and work speed balance
        - Focus on slow but consistent output
    - Citizens will need to reserve the item or job they are working on to prevent conflicting jobs
    - The Outpost Hall is the stockpile (scripts/outpost_hall.gd's new StockpileSpot
      marker, wired via WorldGrid.stockpile_spot in Base._ready()). Workstation
      gained output_buffer + an exported carry_limit (scripts/workstation.gd);
      Farm additionally gained input_buffer + work_interval (scripts/farm.gd).
      Character's old per-tick Timer (Farm) and lumberjack-only coroutine were
      unified into two coroutines - _run_farm_loop and _run_lumberjack_loop - both
      calling a shared _haul_to_stockpile helper (scripts/character.gd) that walks
      to the Outpost Hall, drops off output_buffer, and (Farm only) withdraws up to
      carry_limit of wood capped by what the stockpile actually has. A farm haul
      trip fires when input can't cover the next tick OR output is full, whichever
      first, so one trip covers both directions when possible. A lumberjack now
      caps out at carry_limit per tree visit and releases (doesn't deplete) a tree
      that still has wood left when that happens, so it can be finished later.
      Movement was slowed down (MOVE_SPEED 300->140, MIN/MAX_MOVE_DURATION
      0.2/3.0->0.3/4.0) so a stockpile round-trip reads as real travel time -
      "slow but consistent" per the design note - these are first-pass numbers,
      not iterated on via playtesting.
    - Playtested and confirmed working; fixes applied after playtest feedback:
      1) Lumberjacks now maintain a shared area-wide forest target
      (LumberCamp.optimal_tree_count, checked via WorldGrid.count_trees_near) and
      plant toward it every loop pass regardless of who chopped what, replacing
      the old per-worker "replant N after each harvest I personally finished"
      logic (removed max_cultivated_trees/replant_per_harvest/_cultivated_trees).
      2) optimal_tree_count's default had to be raised from 8 to 16 - the initial
      scatter already seeds ~11-12 trees near the camp, so a default *below* that
      meant the "plant if under target" condition was false from turn one and
      never visibly triggered in a normal playtest; the default now sits above
      the initial population so topping-up is visible immediately. 3) Lumberjacks
      were making a wasted stockpile-then-back-to-camp-then-to-tree round trip -
      _haul_to_stockpile no longer walks the character back to the post it came
      from (only Farm's loop does that explicitly now, since production only
      happens standing at the farm; a lumberjack instead heads straight from the
      stockpile to its next tree/planting spot). 4) Idle citizens (no assigned
      post, including the default state at game start) now automatically haul
      whichever workstation has the fullest output_buffer to the stockpile
      (Character._run_hauler_loop/_find_haul_job), on top of - not instead of -
      each dedicated worker's own self-haul when their own buffer/input hits a
      threshold; both were verified coexisting correctly in the same headless run.
      Idle characters' status label now reads "Hauling" instead of "idle" since
      that's what they're actually doing. 5) Extended to inputs too: any
      Workstation can now declare get_input_resource() (Farm returns "wood",
      the base default is "" meaning none - LumberCamp doesn't override it);
      _find_haul_job picks whichever job (output pickup or input delivery)
      would move more material, and idle haulers carry the needed resource from
      the stockpile out to a starved post via the new _deliver_to_post. Caught
      and fixed a real bug this surfaced: unlike output pickup (a single atomic
      read-and-zero at the stockpile, no travel gap), input delivery computes
      its carry amount at pickup time and only applies it after walking back to
      the post - with multiple idle characters converging on the same starved
      post from an identical "empty" snapshot, their deliveries stacked past
      carry_limit and drained the stockpile well past what was needed.
      _deliver_to_post now re-checks the post's actual remaining space at the
      moment of delivery and refunds any surplus (or an interrupted mid-trip
      carry) back to the stockpile instead of overshooting or destroying it.
      Re-verified headless with 3 idle characters converging on one starved
      farm (capped correctly at carry_limit, refund landed back in the pool)
      and with dedicated Farm+LumberCamp workers plus one idle assistant
      running together (no overshoot, sensible behavior throughout).
    - Job reservation: only WorldTree.claimed (pre-existing) was needed to prevent
      two lumberjacks fighting over the same tree. Did NOT build a general
      job-reservation abstraction beyond that - multiple workers sharing one
      Farm/LumberCamp's buffers is intentional (parallel throughput), and nothing
      else in the game currently has a conflicting-job case to guard against.
    - Known gap: output_buffer/input_buffer aren't covered by the save system
      (Implement_Next.txt item above) - a quickload resets in-transit buffers to
      0 rather than persisting them. Low-impact (self-corrects within a work
      cycle) but worth closing if it turns out to matter.
    - Verified via a temporary headless auto-assign + fixed-timestep run (removed
      before finishing) that exercised ~15 simulated seconds: confirmed a farm
      correctly hauls wood in, produces food, hauls out; confirmed a lumber camp
      chops into its buffer and dumps a full load at carry_limit; confirmed xp,
      resource pool, and worker positions all evolved as expected. Not tested
      interactively in the editor (no GUI in this environment) - recommend an
      in-editor playtest, especially for the visual feel of the slower movement
      and the tree-release-at-carry_limit behavior.

- [DONE] Water Resource
    - Need a well made with stone
    - Infinite water from well
    - Implemented as GameState.water_wells (a count, not a resources[] entry -
      water is explicitly unlimited, not a depletable stockpile) + has_water().
      Well (scripts/well.gd, scenes/building/Well.tscn) is a passive structure
      like House/OutpostHall (no worker assignment), costs 10 stone + 4 wood,
      registered in BuildingCatalog with a "water_wells": 1 field that
      Base._confirm_placement reads the same way population_capacity already
      was. HUD gained a Water: Available/None label (GameState.water_changed
      signal). Saved/restored via GameState.water_wells in Base's save data,
      same pattern as population_capacity. Verified headless: placement costs
      the right stone, has_water() flips true, and it survives a save/load
      round-trip. Not yet consumed by anything - Happiness (below) is what
      will actually read has_water() once built.

- [DONE] Stone Resource
    - Mined from a stone mine
    - StoneMine (scripts/stone_mine.gd, scenes/workstation/StoneMine.tscn)
      extends Workstation with no extra fields - it's the first user of a new
      generic Character._run_generic_work_loop (plain produce-and-haul, no
      special walking/consumption pattern), which required moving
      work_interval from Farm up onto the Workstation base so any future
      simple gatherer can reuse it too. Registered in BuildingCatalog
      (1x1, costs wood only, since nothing costs stone yet but a stone mine
      itself has to be buildable from turn one). "stone" added to
      GameState.resources and to the HUD (generalized HUD's label lookup
      from a hardcoded food/wood ternary to a _resource_labels dict so a
      third resource didn't need more special-casing). Verified headless:
      output accumulates and gets hauled by both the dedicated worker and an
      idle assistant with no special-casing needed in the hauler code -
      confirms the buffer/carry_limit/hauler system genuinely generalizes to
      new Workstation types.

- [DONE] Happiness System
    - Each citizen has a personal happiness
    - Settlement happiness is an average of all citizen's happiness
    - Access to water and food boost happiness while lack of vital resources reduces happiness
    - Citizens may leave the town if their individual happiness is too low for too long
    - Food variety gives improved happiness
    - Per-citizen happiness (CharacterData.happiness, 0-100, persisted like
      skill_xp) eases toward a target Base recomputes every
      HAPPINESS_TICK_INTERVAL (5s) from current settlement conditions -
      GameState.has_water(), whether "food" stock is above 0, and
      GameState.FOOD_RESOURCES variety count (Alternative Crop Types'
      contribution: more distinct food types in stock raises the target) -
      rather than jumping straight to it, so a sudden change (e.g. the well
      running dry) is felt gradually over several ticks, not instantly. Lives
      in Base (scripts/base.gd's _on_happiness_tick and friends) rather than
      GameState, since it needs the character roster GameState has no
      reference to. Settlement happiness (HUD's new Happiness: NN% row) is
      just the average, computed on demand via Base.get_average_happiness().
      A citizen whose happiness stays below UNHAPPY_THRESHOLD (20) for
      LEAVE_AFTER_UNHAPPY_TICKS (12, i.e. 60s) consecutive ticks leaves for
      good - CharacterData.unhappy_streak tracks the consecutive-tick count
      and is persisted specifically so a quicksave/quickload can't be used to
      reset a citizen's countdown. Character.leave() (unassign + stop all
      coroutines) is called before Base queue_free()s them, so nothing tries
      to touch the node after it's gone; population_count drops to match.
      This is the game's first form of permanent citizen loss other than the
      design doc's already-stated perma-death pillar - loading an older save
      that still has a departed citizen won't resurrect them in the current
      session, which is the intended (if blunt) consequence of departure
      being permanent. All these numbers (bonuses, penalties, ease rate,
      thresholds) are a first pass, not iterated on via playtesting.
    - Verified headless: happiness correctly eases toward a computed target
      (not jumping to it) under good conditions (water+food+full variety) and
      persists correctly through a save/load round-trip; under sustained bad
      conditions (no water, no food/variety) a citizen actually leaves at the
      expected tick count, population_count and the HUD label update
      accordingly, and Base.characters.duplicate() correctly protects the
      iteration while _character_leaves mutates the real roster mid-loop
      (multiple citizens leaving in the same tick, which happened in the test
      since all 3 shared identical synchronized history, didn't corrupt
      anything).

- [DONE] Alternative Crop Types
    - Grain
        - Make into flour at a mill
        - Make flour into bread at a bakery
        - Make beer with hops at a brewery
    - Fruit
        - Fruit trees take longer to grow but are consistent
    - Potatoes
        - Consistent and doesn't require additional refinement
    - Rather than 7 new subclasses, generalized Farm (scripts/farm.gd) into a
      configurable single-input/single-output converter - despite the name it
      now covers raw crops AND refinement buildings alike, each just a
      differently-configured Farm instance sharing one loop
      (Character._run_farm_loop). New exported fields: input_per_tick/
      input_resource (was hardcoded "wood"/wood_per_tick), skill_id (was
      hardcoded "farming" - Mill/Bakery/Brewery train "milling"/"baking"/
      "brewing" instead, since sharing a class doesn't mean sharing a trade).
      A no-input crop (Fruit/Potato - "consistent", not gated on deliveries)
      just sets input_per_tick to 0, which makes the loop's restock check
      always pass without any special-casing. All 7 new buildings
      (GrainFarm/Mill/Bakery/HopsFarm/Brewery/FruitOrchard/PotatoFarm) share
      one new scene, scenes/workstation/CropStation.tscn, distinguished
      purely by BuildingCatalog fields - which required adding a sprite_tint
      export to Workstation (scripts/workstation.gd) so they don't all look
      identical despite sharing art. GameState.resources grew grain/flour/
      bread/hops/beer/fruit/potato. GameState.FOOD_RESOURCES (["food",
      "bread", "potato", "fruit"] - grain/flour/hops/beer are intermediate or
      luxury goods, not directly edible) generalizes the hunger-consumption
      timer to draw from any of them in priority order rather than only
      "food"; exposed as a const for the Happiness system to read for its
      "food variety" bonus. Beer/hops/grain/flour are NOT in FOOD_RESOURCES
      and aren't consumed by hunger - beer in particular is left as a future
      Happiness luxury-good input rather than a calorie source.
    - Did NOT add HUD rows for the 7 new resources - the compact corner panel
      already has 5 rows (Food/Wood/Stone/Water/Population); adding 7 more
      wasn't practical for this pass. They're fully functional in
      GameState/production logic, just not individually surfaced in the HUD.
      A more scalable resource display (collapsible list, icon bar) would be
      its own follow-up.
    - Caught and fixed two real bugs while verifying this, neither specific
      to crops: 1) HUD._on_resources_changed crashed on any resource without
      a display label (every new resource type) - it's wired to ALL resource
      changes, not just displayed ones, and indexed straight into a
      dictionary that only had 3 keys. Now bails out early for anything it
      doesn't have a label for. 2) A genuine pre-existing race in
      Character._run_farm_loop, only surfaced now because a 3-stage chain
      made it easy to accidentally point two workers at one post during
      testing: the "is there enough input" check happened before the
      work_interval wait, not right before consuming, so two workers sharing
      one Farm-class post (always allowed, same as LumberCamp) could both
      pass the check, both wait, and both decrement - driving input_buffer
      negative. Fixed by re-checking immediately before the actual
      consumption; a losing check now just skips that cycle instead of
      corrupting the buffer.
    - Verified headless: a real GrainFarm->Mill->Bakery chain run confirmed
      grain flowing to flour to bread with no negative buffers anywhere, and
      confirmed bread got drawn down by hunger once "food" ran dry (matching
      FOOD_RESOURCES priority order). Separately verified a PotatoFarm
      produces and gets consumed by hunger without ever attempting to haul
      in an input (input_buffer stays 0 throughout).

- [DONE] Storage Facility
    - Determines how much storage the town has
    - Can have multiple, each increasing storage capacity
    - Can also be upgraded for additional capacity
    - 2 x 4 size
    - GameState.resources previously had NO upper bound at all - add_resource
      only ever clamped the floor at 0. Added GameState.storage_capacity
      (baseline 30, see BASE_STORAGE_CAPACITY), now clamped on both ends in
      add_resource; the cap applies to every resource type independently
      (a full food stockpile doesn't crowd out wood/stone). Note: a haul
      dropping off more than the remaining headroom silently loses the
      overflow rather than refunding it to the workstation - unlike the
      earlier input-hauler bug, this is an intentional consequence of the cap
      existing at all (build more storage or spend down what you have), not
      an accounting error. StorageFacility (scripts/storage_facility.gd,
      scenes/building/StorageFacility.tscn) is a passive structure like
      House/Well, 2x4, grants +30 via a "storage_capacity" catalog field read
      the same way population_capacity/water_wells already were. HUD resource
      labels now show "Food: 12/30" format; capacity changes refresh all of
      them since the cap is shared across resource types, not per-label.
      Saved/restored via GameState.storage_capacity, same pattern as the
      other capacity fields. Did NOT implement the "can also be upgraded"
      part - that needs a new building-selection/upgrade interaction that
      doesn't exist yet (Storage Facility isn't clickable post-placement,
      unlike Farm/LumberCamp which are selectable via the character
      assignment flow) and felt like it deserved its own design pass rather
      than a one-off bolted onto this feature; "place multiple, each adds
      capacity" was implemented and covers the core ask. Verified headless:
      clamping works, placing a facility raises the cap and immediately
      allows further filling, and it survives a save/load round-trip.

- [DONE] Citizen recruitment system
    - Pay food to recruit a new citizen from the outpost hall
    - Choose between 3 options
        - Different starting skills
    - Outpost Hall was a plain Node2D (a stockpile drop-off point, never
      clicked); became an Area2D with a `clicked` signal
      (scripts/outpost_hall.gd, matching Character's own input_event
      pattern) so Base can open a new RecruitPanel on click - a near-exact
      clone of BuildMenu's slide/fade/number-key-shortcut UI pattern, showing
      3 candidates generated by a new static RecruitCatalog (name pool +
      random specialization skill_id, each starting at level 5 in that skill
      via SkillCurve.xp_for_level - "different starting skills" per the
      design doc). Recruiting is gated on both population_capacity (needs an
      empty House slot) and affording RecruitCatalog.RECRUIT_COST (15 food).
      Base._spawn_character is the actual instantiation path - a fresh
      CharacterData (not a shared .tres like Aldric/Brenna/Cass, since
      recruits are procedural) on a freshly instantiated Character.tscn,
      added as a direct Base child (required for y-sorting) and appended to
      `characters`, matching exactly how the 3 starting citizens are wired in
      _ready().
    - This forced a real fix to the save system's character handling:
      _restore_characters previously ontextly matched saved entries against
      whoever was already in the live `characters` array and silently
      dropped anything else - fine when the roster was fixed, but a
      recruited citizen wouldn't have existed yet on a fresh game boot (only
      the 3 originals start in the scene file), so they'd be permanently
      lost even with a save file that had their data. Now recreates any
      unmatched saved entry via _spawn_character. This also reverses (and
      corrects) an assumption made while building the Happiness System: that
      a departed citizen should never come back via load. Loading a save is
      reverting to that point in time, so a citizen who left *after* the
      loaded save was made should reappear, same as any other town state a
      load reverts - the earlier code comment claiming otherwise was wrong
      and has been corrected.
    - Verified headless: recruiting correctly denies with no housing, denies
      with insufficient food, and succeeds otherwise (food spent, skill_xp
      granted matching the chosen specialization, population_count
      incremented, HUD-visible). Confirmed the fresh-boot gap is actually
      closed: saved with a 4th (recruited) citizen present, removed them from
      the live roster without touching the save (simulating a closed-and-
      reopened game), reloaded, and confirmed they were recreated with their
      correct skill_xp intact.
    - Post-review fix: the first pass only ever *added* missing citizens on
      load and matched by character_name, which had two real gaps -
      1) loading an older save never removed a citizen recruited *after*
      that save was made (they'd stay in the world and population_count
      would silently follow characters.size() instead of the save), and
      2) RecruitCatalog's small name pool meant two live citizens could
      collide on the same name, which name-based matching would silently
      mismatch on load. Fixed by giving CharacterData a stable `id` (backfilled
      for Aldric/Brenna/Cass from their node name, generated fresh for every
      recruit) and matching save entries on id instead of name; a live
      citizen with no matching save entry is now removed on load, symmetric
      to a saved entry with no live match being recreated. Saves without an
      "id" field (pre-fix) fall back to name matching for one-time migration.

- [DONE] Show resource income per minute on the HUD
    - GameState.get_income_per_minute(resource_name) is computed from a
      rolling history of resource-pool snapshots (GameState._income_history,
      one sample/second, trimmed to the trailing 60s) rather than watching
      individual add_resource() calls - production arrives in bursty
      haul-trip chunks (a lumberjack/hauler dumps several units at once,
      then nothing for seconds), so a per-call rate would spike and drop
      instead of reading as a steady number. The rate is net change over
      however much of that 60s window has actually elapsed (shorter right
      after boot), i.e. net income actually reaching the stockpile - a
      workstation losing output to a full storage cap won't show up here,
      since nothing reached the resource pool to measure.
    - HUD.gd's existing Food/Wood/Stone labels now append "(+N.N/min)" (or
      "-N.N/min") alongside the amount, refreshed on its own 1s timer
      independent of the amount-change tween, so the rate keeps updating
      even during a stretch with no resource change events. Widened the
      whole HUD corner panel (196->260px) and its labels to fit the longer
      text without clipping/overflowing.
    - Verified headless: assigned a citizen to the starting Lumber Camp,
      let it run 25 real seconds, and confirmed get_income_per_minute("wood")
      matched the actual measured delta/elapsed*60 exactly (wood rose 10->14,
      reported rate ~9.6/min). Not verified in-editor for the visual/label
      layout - recommend a quick playtest to confirm the widened panel and
      appended rate text read well against the existing punch/tween juice.
    - Post-review fix, from an explicit request to double-check this against
      the actual gathering rate: found a real (if small) systematic
      over-count. _sample_income_history's old trim policy dropped the
      oldest sample as soon as *it* crossed the 60s boundary, so the
      retained "oldest" sample's true age could be anywhere up to one full
      INCOME_SAMPLE_INTERVAL (1s) past 60s - close enough to look right,
      but a real bias for a bursty producer (haul trips arrive in discrete
      chunks, not a smooth trickle), averaging up to ~1.67% over-counted
      and confirmed empirically at ~1.5-3% in a live test. Fixed two ways:
      the trim policy now keeps the oldest sample until the *next* one has
      also crossed the boundary (guaranteeing a valid "before" anchor
      at-or-before "now - 60s" always exists once that much history has
      accumulated), and get_income_per_minute now interpolates the
      resource value at exactly "now - 60s" between whichever two samples
      straddle it, rather than reading the coarse discrete sample that
      happened to still be retained. Net effect: the window is now exactly
      60.000s (not "somewhere between 60 and 61s") regardless of how
      coarse INCOME_SAMPLE_INTERVAL is.
    - Verified headless with an independent cross-check using a *separate*
      0.1s-resolution log of the same resource, picking whichever
      independent sample landed closest to "now minus 60s" rather than
      assuming a fixed sample count - before the fix, reported rate ran
      ~1.5-3% above this independent measurement at every checkpoint
      (consistently over, not random noise, matching the theorized bias);
      after the fix, reported and independent rates matched to within
      floating-point/timing noise (e.g. 20.0 vs 19.99, 8.1 vs 8.10) once
      past the initial 60s ramp-up.

- [DONE] Condense build menu into square icons with small keybind text
    - ButtonContainer switched from a VBoxContainer of full-width text
      buttons to a 3-column GridContainer of fixed 64x64 square buttons
      (scenes/ui/BuildMenu.tscn); the whole panel was resized to fit 5 rows
      (14 placeable options today). Each button shows a TextureRect icon
      (stretched/centered to fit) plus a small keybind number label pinned
      to the top-left corner (only for the first 9 options, matching the
      existing 1-9 shortcut limit) - full building name + cost moved to
      the button's tooltip_text since neither fits at this size.
    - Icons are read directly off a throwaway instance of each option's own
      scene (scripts/build_menu.gd's _get_icon_data/_icon_cache) - the
      instance gets Base.BUILDING_PROPERTIES applied exactly like a real
      ghost/placement does, is added to the tree just long enough for its
      _ready() to pick the correct RESOURCE_VISUALS texture and sprite_tint,
      then is immediately freed - rather than hand-duplicating that
      texture-selection logic here, which would silently drift if the
      catalog ever changed. Cached per option id so this only happens once
      per building type, not once per menu open. Several buildings
      currently share the same generic silhouette texture underneath their
      tint (pre-existing art reuse, not introduced by this change) - the
      icons accurately reflect that, they're not meant to paper over it.
    - Verified headless: opened the build menu programmatically and
      confirmed all 14 placeable options resolved a non-null icon texture
      with no errors. Not verified in-editor for visual quality (icon
      legibility at 64px, keybind label contrast, tooltip readability) -
      recommend a playtest pass, especially since several buildings will
      look identical or near-identical at a glance (see above).
    - Post-review fix, from actual in-editor feedback: the icon approach
      above was confirmed unreadable in practice, exactly as the risk noted
      above predicted - 9 of the 14 buildings share one generic silhouette
      texture differing only by tint, and even the buildings with distinct
      art were drawn for a zoomed-in world view, so shrinking either down
      to 64px produced an illegible, near-identical smudge rather than
      something a player could identify at a glance. Replaced the
      TextureRect icon entirely with a solid-color ColorRect background
      (the building's own sprite_tint, darkened so white text stays legible
      on top; a flat neutral for anything with no custom tint) plus a bold,
      centered Label showing the first word of the building's display_name
      ("Grain Farm" -> "Grain", "Stone Mine" -> "Stone" - checked all 14
      current entries for collisions; there are none, and every one reads
      as a clear, self-contained identification without needing the hover
      tooltip at all). Applied identically to CropPanel (scripts/
      crop_panel.gd), which shared the exact same icon approach and problem
      for its 8 Farm-family options. Also added `clip_contents = true` to
      the button so nothing can ever bleed past the square's own edges.
      Removed the throwaway-scene-instantiation icon cache entirely from
      both panels - no longer needed, and the sprite_tint field is already
      sitting right in the catalog dict, so this is simpler and more
      robust than before, not just prettier.
    - Verified headless: derived label text for all 14 build-menu options
      and all 8 crop-panel options, confirmed every button has both a
      background and a label with no duplicate text and clip_contents
      correctly set. Not verified in-editor for the actual visual result -
      recommend a playtest to confirm this reads as clearly as intended.

- [DONE] Increase the size of the map
  - At least 4 times current size
    - Pure data change - WorldGrid's bounds and RtsCamera's pan/edge-scroll
      limits were already computed dynamically from IsoGround's exported
      width/depth/start_x/start_y in Base._ready(), so this was just
      changing IsoGround's overrides in scenes/base/Base.tscn: 14x14 (196
      tiles) -> 28x28 (784 tiles), exactly 4x the area. start_x/start_y
      moved from -5/-5 to -12/-12 (not simply left at -5) so the map grows
      symmetrically around the existing fixed buildings rather than only
      outward in the positive direction, keeping Outpost Hall/Farm/Lumber
      Camp centered in the bigger map the same way they were centered in
      the old one. No code changes anywhere - no script hardcodes the old
      14/196 size.
    - Verified headless: tile count is 784, WorldGrid.bounds_min/max read
      (-12,-12)/(15,15) as expected, and RtsCamera's limit_left/top/right/
      bottom scaled up accordingly with no errors. Not verified in-editor
      for feel (camera pan/zoom over the larger area, initial tree scatter
      density now looking sparser relative to the bigger map) - recommend
      a playtest, and consider raising INITIAL_TREE_COUNT/scatter radius
      in a follow-up if the starting area reads too empty.

- [DONE] Add skills for speed and strength
  - Movement speed and carry weight respectively
  - Trained by moving and carrying things
    - Two universal skills every citizen trains passively regardless of
      assignment (unlike farming/lumberjacking/etc., which only train while
      working a specific post) - they ride the same SkillCurve/multiplier
      system, just with different triggers and effects.
      Character._move_to() now scales MOVE_SPEED by
      get_skill_multiplier("speed") and grants speed xp proportional to the
      move's duration (not a flat per-move amount) - this keeps xp/real-
      time-spent-moving roughly constant regardless of level, the same
      "reward time, not output" principle XP_PER_GATHER already follows for
      gathering, applied to movement (a faster mover finishes each trip
      sooner but makes proportionally more trips in the same time, rather
      than snowballing).
    - New Character._carry_capacity(post) = post.carry_limit *
      get_skill_multiplier("strength") replaces every carry_limit reference
      inside character.gd's haul/work-loop functions (pickup/delivery
      amounts, the output-buffer-full haul trigger in all three work loops,
      and the lumberjack's per-tree carry-out cap) - post.carry_limit itself
      stays the building's own base stat, unchanged. A stronger character
      moves more per trip and needs fewer trips to fully service a post;
      strength xp (flat, like XP_PER_GATHER) is granted once per haul trip
      that actually moved something, on either leg.
    - Neither skill is shown in the UI yet (Character's status label only
      shows the currently-assigned post's trained skill) - surfacing them
      is exactly what the planned per-citizen skill panel (below) is for.
    - Verified headless: ran a lumberjack for 90 real seconds and confirmed
      both xp types accumulate from ordinary play (speed reached level 2
      from walking alone; strength gained xp per haul trip, correctly not
      yet enough for level 2 in that window) with no errors. Separately
      confirmed the multiplier math directly: at a synthetic level 20
      (1.38x), carry capacity read 8.28 against a base carry_limit of 6.0,
      and move duration for a fixed 300px trip dropped from ~2.14s to
      ~1.55s - both matching hand-computed expected values exactly. Not
      verified in-editor for feel (how noticeable the speed-up/carry boost
      reads visually at low levels, and whether the new frequent early
      level-ups from movement alone feel like sound/punch-effect spam) -
      recommend a playtest.

- [DONE] Add bonuses and debuffs to certain global happiness levels
    - Settlement happiness (Base.get_average_happiness()) now falls into one
      of 4 bands (Base.HAPPINESS_BANDS): Thriving (>=80, 1.15x), Content
      (>=50, 1.0x - the existing baseline target), Unhappy (>=20, 0.85x),
      Miserable (<20, 0.6x - same threshold as UNHAPPY_THRESHOLD, where a
      citizen is already accumulating toward leaving). Recomputed every
      happiness tick alongside the existing per-citizen easing, and stored
      as GameState.happiness_output_multiplier - a flat multiplier every
      Workstation-family work loop in character.gd now applies on top of
      the worker's own skill multiplier when adding to output_buffer
      (Farm-class converters, generic loop, lumberjack chopping). Only
      output is affected, not input consumption - an unhappy worker still
      draws the same input but wastes more of it producing less, which
      reads as a productivity/carelessness debuff rather than a hunger
      change. HUD's happiness label now shows the band name alongside the
      percentage ("Happiness: 87% (Thriving)") so the effect isn't invisible.
    - Verified headless: forced settlement happiness to opposite extremes
      (all citizens to 90, then to 10) a tick apart and confirmed
      GameState.happiness_output_multiplier and the HUD label both updated
      to the correct band (1.15x/Thriving at 87% eased-from-90, 0.6x/
      Miserable at 13% eased-from-10) with no errors. Did not re-verify the
      actual production-rate change end-to-end (trusted the multiplication
      itself, already covered by the income/minute and skill-multiplier
      tests earlier) - not verified in-editor for feel/balance (whether
      +/-15-40% swings read as noticeable without being punishing) -
      recommend a playtest.

- [DONE] Be able to select what crop to grow by selecting a farm
    - Clicking any already-placed Farm-family building (the original
      wood->food Farm, or any Alternative Crop Types building - all share
      the Farm class, see farm.gd) now opens a new CropPanel (near-exact
      clone of BuildMenu's icon-grid pattern, see scripts/crop_panel.gd/
      scenes/ui/CropPanel.tscn) listing every Farm-family catalog option
      (BuildingCatalog.farm_family_options(), found structurally by which
      scene an option instantiates rather than a hardcoded id list).
      Picking one is a free, instant retool - not a rebuild - via
      Base._apply_crop_option(), reapplying the same
      _apply_option_properties used at placement time directly onto the
      existing node (resource_type/input_resource/input_per_tick/
      output_per_tick/skill_id/sprite_tint/work_interval). An already-
      assigned worker doesn't need to be interrupted: Character's work
      loops re-read these fields fresh every loop iteration rather than
      caching them, so the next production tick just uses the new recipe.
      output_buffer/input_buffer are cleared on an interactive retool
      (_on_crop_selected) - whatever was accumulated under the old recipe
      doesn't carry over (e.g. half a load of grain about to become a
      Bakery's flour input) - an accepted loss, same spirit as storage
      overflow being lost elsewhere in this game rather than an accounting
      bug.
    - New Workstation.refresh_visual() (scripts/workstation.gd) extracts the
      sprite texture/tint logic previously inlined in _ready() so it can be
      re-run after a runtime field change; a new _default_texture/
      _default_centered/_default_offset captured before the first
      RESOURCE_VISUALS override lets it correctly revert to the scene's own
      baked-in sprite when switching to a resource_type with no
      RESOURCE_VISUALS entry (e.g. "grain"), rather than getting stuck on
      whatever a previous resource_type happened to pick.
    - Real gap caught and fixed while implementing this: both save and load
      rebuild a Farm-family building's fields from a stored catalog
      option_id, not by reading the live node - a retool would otherwise
      silently revert on the next save/load round-trip. Fixed two ways: a
      player-placed building's _placed_buildings entry now has its
      option_id updated in place on retool (the existing save/load path
      already rebuilds correctly from there); the one fixed scene-file
      Farm ($Farm, not tracked in _placed_buildings at all) gets a new,
      minimal save field (Base._fixed_farm_option_id) reapplied on load
      before post_buffers are restored (so the load's actual saved buffer
      values are authoritative, not the empty-buffer reset an interactive
      retool wants).
    - Did NOT restrict which Farm-family options are offered based on the
      clicked building's own current recipe (e.g. a Bakery can be retooled
      directly into a raw Fruit Orchard, not just other refinement steps) -
      all 8 share one class and one worker-assignment slot, so any-to-any
      felt like more player agency than it did a design risk, and scoping
      it down would need a new "raw crop vs. refinement" catalog
      distinction that doesn't exist yet. Also did NOT highlight/disable
      the currently-active option in the panel - every retool (including
      re-picking the current recipe, a harmless buffer-clearing no-op) is
      offered uniformly; tracking "which option is this node currently" for
      display purposes only felt like unnecessary bookkeeping for this pass.
    - Verified headless: clicking a Farm-family building opens the panel
      with all 8 options and a valid icon for each; selecting one correctly
      reconfigures resource_type/input_resource/skill_id/sprite_tint and
      clears both buffers; the visual-revert case (switching away from a
      RESOURCE_VISUALS-covered type back to the generic default) was
      confirmed by direct texture comparison. Separately verified full
      save/load round-trips for both a retooled fixed $Farm and a retooled
      player-placed building - both correctly restored the NEW recipe
      (fields, skill_id) and the saved buffer amounts exactly, not the
      pre-retool recipe or a cleared buffer. Not verified in-editor for
      feel (panel positioning/overlap with other bottom-left panels,
      whether "click a building to reconfigure it" reads intuitively
      alongside "click a building's worker to assign them" - especially
      relevant once the planned task-list removal below changes how
      workstation clicks are otherwise handled) - recommend a playtest.
    - Post-review fix: giving Farm an input_event/clicked handler broke
      placing any Farm-family building via the build menu (and, as a
      pre-existing latent bug from the earlier recruitment commit, placing
      a second Outpost Hall too - it's placeable and has had the same kind
      of handler since). The placement ghost is a live instance of the
      real scene, so it follows the cursor and its own Area2D collision
      shape sits right where a confirming click lands; that click fired the
      ghost's own input_event first, which calls set_input_as_handled()
      before Base._unhandled_input() ever sees it to run
      _confirm_placement() - the same interception mechanism
      _on_farm_clicked/_on_outpost_hall_clicked rely on to stop a click
      from also deselecting, working against placement here instead of for
      it. Headless verification couldn't have caught this - it requires
      actual physics-picking/mouse input, not just checking the resulting
      game state. Fixed generically rather than special-casing Farm:
      Base._on_build_option_selected now sets
      `ghost.input_pickable = false` for any ghost that's a
      CollisionObject2D, so a future clickable post type placed via the
      build menu won't reintroduce this.

- [DONE] Maximum number of citizens per workstation
    - Generalized a pre-existing but hardcoded special case rather than
      building something new: WallSegment already capped itself at 1
      worker, but only via a literal `post is WallSegment and
      post.active_workers >= 1` check inlined at both of Base's assignment
      entry points (drag-drop in _end_character_drag, and TaskPanel's
      assignable-list filter in _on_character_selected) - Workstation-family
      posts (Farm/LumberCamp/StoneMine) had no cap at all. Added
      `max_workers` as an export on both Workstation (default 3 - a
      first-pass number, not tuned via playtesting; multiple workers
      sharing one post's buffers for parallel throughput is intentional
      per CLAUDE.md, so this isn't 1) and WallSegment (default 1,
      preserving its exact prior behavior), duck-typed the same way
      add_worker/remove_worker/get_worker_spot/display_name already are.
      Both call sites now go through one new Base._post_has_room(post,
      for_character) helper instead of the inlined WallSegment check.
    - _post_has_room() always returns true for a post the character being
      considered is *already* assigned to, even if that post is at its cap
      - re-dropping a worker back onto their current post (a no-op
      reassignment) shouldn't read as "denied" just because they themselves
      count toward its own occupancy at the moment of the check.
    - The cap is enforced only at these two UI entry points, same as
      WallSegment's cap always was - Character.assign_to()/Workstation.
      add_worker() themselves have no denial logic and will accept a
      worker unconditionally if called directly (e.g. _restore_characters
      during load). This is a deliberate continuation of the existing
      pattern, not a gap introduced here - an old save from before this cap
      existed, with more workers on one post than the new max_workers,
      would just carry that over (grandfathered) rather than being
      force-corrected on load.
    - Verified headless: assigned 3 characters to the starting Farm
      (max_workers 3, succeeds), confirmed a 4th spawned character is
      denied room there, and confirmed an already-assigned character still
      reads as having room on that same now-full post. WallSegment's
      analogous behavior wasn't separately re-verified live (no wall
      segments are currently placed anywhere in the game - see CLAUDE.md's
      note on the wall-building concept being unfinished/unwired) but is
      structurally identical to Workstation's already-verified path. Not
      verified in-editor.

- [DONE] Remove current task list UI from citizens
  - Have to be assigned by clicking and dragging or clicking on the citizen and then the workstation
- [DONE] Implement skill level UI to citizens, to view all of their skill levels in a condensed layout when clicked on
    - Implemented together since both change what clicking a citizen does.
      TaskPanel (scripts/task_panel.gd, scenes/ui/TaskPanel.tscn) is deleted
      outright, not just unwired - replaced by a new SkillPanel (scripts/
      skill_panel.gd, scenes/ui/SkillPanel.tscn, near-identical slide/fade
      shell to the other panels but a plain list of 8 "Skill: Lv N" labels,
      not buttons - there's nothing to choose here). Clicking a citizen now
      selects them and opens SkillPanel showing all 8 skills (the 6
      work skills plus speed/strength), always all 8 including untrained
      ones at level 1 so the layout doesn't reflow.
    - Assignment is now: (1) drag-and-drop, unchanged, or (2) click a
      citizen to select them, then click a workstation/wall to assign them
      there - denied with a HUD flash if that post is already at its
      max_workers (see the worker-cap item above). Clicking the *same*,
      already-selected citizen again unassigns them (sets them idle) -
      the replacement for the old task menu's "[0] Idle" option.
    - Resolving "click a post while a citizen is selected" needed two
      different mechanisms depending on the post: a Farm-family building
      already intercepts its own click for the crop-selection panel (see
      the crop-selection item above), so Base._on_farm_clicked now checks
      _selected_character first and assigns instead of opening that panel
      when one exists. Every other assignable post type (LumberCamp,
      StoneMine, WallSegment) has no click handler of its own, so a new
      generic branch in Base._unhandled_input resolves the click via the
      same _post_at() physics point query drag-and-drop already uses.
      Both paths share one new Base._assign_selected_to(post) helper.
    - Verified headless: selecting a citizen opens the skill panel with
      the correct name; re-clicking the same selected citizen unassigns
      and deselects; clicking a non-Farm post (Lumber Camp) while selected
      assigns correctly; clicking a Farm-family post while selected assigns
      there instead of opening the crop panel; filling a Farm to its
      3-worker cap and then selecting-and-clicking a 5th citizen onto it
      correctly denies the assignment; clicking a Farm-family post with
      nothing selected still opens the crop panel as before. Not verified
      in-editor for feel (whether the skill panel's positioning/timing
      reads well, whether "click twice to unassign" is discoverable without
      the old menu spelling it out) - recommend a playtest.

- [DONE] Main menu with save/load UI (ad-hoc request, not from the list above)
    - The game now boots into a new MainMenu scene (scenes/menu/MainMenu.tscn,
      project.godot's run/main_scene) instead of straight into Base.tscn:
      Continue (loads whichever save has the newest timestamp, skipping the
      slot picker), New Game, Load Game, and Quit. New Game/Load Game open
      a new shared SlotPanel (scenes/ui/SlotPanel.tscn) listing 3 save
      slots with a status line each ("Empty" or timestamp + population);
      picking one sets SaveManager.active_slot/should_load_on_start and
      switches to Base.tscn via change_scene_to_file.
    - SaveManager (autoload/save_manager.gd) went from one fixed save path
      to SLOT_COUNT (3) numbered files under user://saves/, plus
      has_any_save()/most_recent_slot() for MainMenu's Continue button;
      SlotPanel reuses has_save()/load_game() directly to build each
      slot's preview text rather than needing a dedicated summary API.
      One-time migration: an existing pre-slot-system user://savegame.json
      is moved into slot 1 on first boot after this change, rather than
      becoming silently unreadable.
    - In-game, save/load is no longer only F5/F9: a new SystemMenu panel
      (Resume/Save Game/Load Game/Main Menu/Quit to Desktop) opens via Esc
      - when nothing else is selected/open, so Esc still backs out of a
      selection/panel first like before - or a new "Menu" button on the
      HUD. Save/Load from here open the same SlotPanel (in "save"/"load"
      mode); whichever slot gets picked also becomes
      SaveManager.active_slot, so a subsequent F5/F9 keeps acting on it.
      F5/F9 themselves are unchanged in spirit, just now parameterized by
      active_slot instead of a single hardcoded path.
    - Real cross-scene correctness gap this surfaced and fixed: GameState
      and the three fixed citizens' (Aldric/Brenna/Cass) CharacterData are
      not part of Base's node tree - they're an autoload and shared .tres
      resources respectively, so nothing about switching MainMenu <-> Base
      resets them on its own the way recreating Base itself resets
      Base's own instance vars. Without an explicit reset, starting a
      second "New Game" in the same process run would have silently kept
      the first playthrough's resources/population/skill progress. Added
      GameState.reset_to_defaults() and an unconditional reset of the 3
      fixed characters' skill_xp/happiness/unhappy_streak at the top of
      Base._ready(), both called before any load-on-start check - safe
      even when a load *is* about to happen, since _load_game()/
      _restore_characters() overwrite every one of these fields from the
      save regardless of what they were reset to first.
    - Verified headless throughout: SaveManager's per-slot save/load/
      migration; GameState + fixed-character reset actually taking effect
      across a real scene reload (mutated both, reloaded the scene,
      confirmed both back to pristine defaults); MainMenu's button
      enabled/disabled states and SlotPanel wiring; the in-game System
      Menu -> SlotPanel -> save/load round-trip (save to slot 2, mutate,
      load slot 2, confirmed restored). Also ran a standalone external
      SceneTree-script driver (not tied to either scene, so it survives
      the scene changes it's testing - a coroutine on MainMenu/Base itself
      can't outlive its own node's replacement) exercising the complete
      real flow end-to-end: New Game -> save -> Quit to Main Menu ->
      Continue -> confirmed the saved value came back correctly. Not
      verified in-editor for feel/layout (SlotPanel and SystemMenu's
      centered positioning, MainMenu's visual polish, whether "Esc opens
      the system menu" reads as expected) - recommend a playtest.
    - Post-review fix: SlotPanel.open_for()'s own doc comment claimed an
      occupied slot "just says it'll be overwritten" in "new"/"save" mode,
      but _rebuild_slots()/_slot_summary() never actually generated that
      text - an occupied slot rendered identically regardless of whether
      the panel was opened to load it, overwrite it with a new game, or
      overwrite it by saving. Concretely: New Game -> pick an occupied
      slot showed the exact same "[1] 2026-07-10 14:32 - Pop 4" text a
      Load Game prompt would, with no visual distinction, so a player
      could pick it thinking they were continuing that save and only
      discover the overwrite once they next saved. Fixed by appending
      " (will overwrite)" to an occupied slot's button text whenever
      _mode != "load".

- [DONE] Unify Farm-family skills back to one "farming" skill (ad-hoc request)
    - Alternative Crop Types had deliberately split Mill/Bakery/Brewery into
      their own "milling"/"baking"/"brewing" skills, reasoning that
      "sharing a class doesn't mean sharing a trade" - reverted per an
      explicit request that every type of farm should train the same
      skill. Farm.gd's skill_id export is gone; get_skill_id() now just
      returns the literal "farming" for every Farm-family instance,
      regardless of recipe. Removed "skill_id" from BuildingCatalog's 7
      crop-chain entries (all of them already either said "farming" or
      one of the three now-gone skills) and from Base.BUILDING_PROPERTIES
      (nothing left to override per-catalog-entry). RecruitCatalog.
      SPECIALIZATIONS and SkillPanel.SKILL_DISPLAY_ORDER/SKILL_LABELS
      dropped milling/baking/brewing too, since nothing trains them
      anymore - down to farming/lumberjacking/mining plus speed/strength.
    - A save with old milling/baking/brewing skill_xp entries loads fine -
      they just sit unused in CharacterData.skill_xp, no migration needed
      (SkillCurve.get_skill_level() still works for any skill_id, trained
      or not; they simply never gain further xp or show up in the skill
      panel anymore).
    - Verified headless: instantiated all 8 Farm-family catalog options
      (including Mill/Bakery/Brewery) and confirmed get_skill_id() returns
      "farming" for every one; confirmed RecruitCatalog now only offers
      farming/lumberjacking/mining as recruit specializations.

- [DONE] Drag a citizen onto a storehouse or Outpost Hall to assign as a hauler
    - The Outpost Hall and Storage Facility now expose the same "post"
      interface as Workstation/WallSegment (add_worker/remove_worker/
      get_worker_spot/active_workers/max_workers, plus a pulse tween on
      the first assigned worker) so both drag-drop and click-then-click
      assignment work on them exactly like any other post - no special
      casing needed in Base's generic _post_at()/_post_has_room()/
      _assign_selected_to() dispatch. StorageFacility had to be converted
      from a plain Node2D to an Area2D with a real CollisionShape2D first -
      it had no collision shape at all, so it was never detectable by the
      physics-point-query drag-drop resolution to begin with. Both cap at
      10 workers (generous vs. Workstation's default 3 - haulers don't
      share a production buffer, so there's no throughput reason to
      bottleneck it, and unassigned/idle citizens already haul with no
      cap at all today).
    - Character.assign_to() gained a branch for these two post types that
      calls _start_hauling() - the exact same coroutine an unassigned
      citizen already runs by default - so this just makes "hauler" a
      real, visible, capped, savable assignment (assigned_post actually
      set, not null) instead of only ever an implicit fallback for having
      nothing else to do. _update_label() shows "Hauling" for this
      assignment too (not the raw building name), since the action is
      what's worth surfacing, not which building they're stationed at.
      Outpost Hall's own click handler (previously always opening the
      recruit panel) now checks for a selected citizen first and assigns
      instead, same priority rule Farm-family buildings already follow
      for the crop-selection panel.
    - Outpost Hall is now registered in Base.posts (with a "outpost_hall"
      save_id, handled in _post_by_save_id like the other two fixed
      buildings) so save/load round-trips a hauler assignment correctly;
      a placed Storage Facility already gets a save_id automatically via
      the existing generic placed-building path, needing no extra wiring.
    - Verified headless: click-assigning a citizen to the Outpost Hall and
      drag-equivalent-assigning another to a freshly placed Storage
      Facility both correctly set assigned_post, incremented
      active_workers, and showed "Hauling" in the status label; filling
      the Outpost Hall to its 10-worker cap correctly denied an 11th;
      a full save/load round-trip correctly restored a citizen's Outpost
      Hall hauler assignment after manually unassigning them in between.
      Not verified in-editor for feel (the pulse animation, whether
      dragging onto these specific buildings reads as intuitively
      "become a hauler" without any on-screen hint) - recommend a playtest.

- [MOVED] Unique art for every building aligning with current design standards
    - Migrated to Outpost_Survival/Ideas/Unique Building Art.md - see below.

- [DONE] Calculate income per minute for resources constantly looking back at the last minute of income rather than when turn ins happen
    - Already addressed by an earlier post-review pass this session (see
      the income/minute commit further up): GameState._income_history
      samples every 1s continuously on its own timer regardless of
      whether a haul trip ("turn in") happened, and get_income_per_minute
      interpolates to an exact trailing 60.000s window rather than
      whatever the nearest discrete sample happens to be. No further
      change needed.

- [DONE] Fix stone miner not returning to mine after drop off at storehouse
    - Real bug in Character._run_generic_work_loop (used by StoneMine and
      any other simple gatherer, not just Farm/LumberCamp which have their
      own dedicated loops): _haul_to_stockpile() deliberately leaves the
      character standing at the stockpile afterward (by design, for
      LumberCamp - heads straight to its next tree rather than detouring
      back through the camp first), but the generic loop never walked them
      back to the post afterward the way Farm's loop already does. Once a
      Stone Mine worker's first haul trip completed, every subsequent
      production tick "happened" from wherever they were left standing at
      the stockpile, forever - the mine itself sat empty and unattended.
      Fixed by adding the same _move_to(post.get_worker_spot()) step
      Farm's loop already has, right after a successful haul trip.
    - Verified headless: placed a Stone Mine, forced a haul trip (set
      carry_limit to 1.0 so it triggers almost immediately), and confirmed
      the worker's position settled back at the mine's worker spot
      (within ~1.5px) rather than staying at the stockpile.

- [DONE] Buildings should show how many of their worker slots are filled in the text field above them
  - Most buildings should only allow 1 worker max
    - Workstation.max_workers default dropped from 3 to 1 (a first-pass
      default, not tuned via playtesting - individual catalog entries can
      still override it upward for genuine parallel-throughput cases).
      WallSegment was already 1. OutpostHall/StorageFacility stay at 10 -
      haulers don't share a production buffer the way workers at one post
      do, so there's no throughput reason to bottleneck them.
    - Each of Workstation/WallSegment/OutpostHall/StorageFacility gained an
      _update_label() that sets label.text to "%s\n%d/%d" %
      [display_name, active_workers, max_workers], called from _ready()
      (and Workstation's refresh_visual(), for the crop-selection-at-
      runtime path) plus every add_worker()/remove_worker() so the count
      stays live as workers come and go.
    - Verified headless: placed a Lumber Camp (max_workers=1), confirmed
      its label read "Lumber Camp 0/1" before assignment and "Lumber Camp
      1/1" after assigning one worker; confirmed a second assignment
      attempt was correctly denied (_post_has_room returned false); and
      confirmed Outpost Hall's label still reads "Outpost Hall 0/10",
      preserving its higher cap.

- [DONE] Farm efficiency buff when well is built
  - I'm thinking more production for the same wood cost
    - Character.WATER_FARM_OUTPUT_BONUS (1.25x) applied in _run_farm_loop
      to farm.output_per_tick only, alongside the existing skill/happiness
      multipliers - input_needed/input_buffer consumption is untouched,
      so this is purely "more output for the same input," not a cost
      discount. Active whenever GameState.has_water() is true; binary
      (no easing) since water access itself doesn't gradually change,
      unlike the happiness multiplier it sits next to.
    - Verified headless: isolated a single production tick from haul-trip
      timing noise (huge carry_limit, pre-filled input_buffer) and
      confirmed output_buffer went from 1.0 (no well) to exactly 1.25
      (after building a well) for the same tick.

- [DONE] Seperate brewing, milling, and baking from farming skill
    - Reverts last turn's unification (confirmed with user: that was a
      misunderstanding - milling/baking/brewing aren't really farming).
      Farm.gd's skill_id export is back, defaulting to "farming";
      BuildingCatalog's mill/bakery/brewery entries set it back to
      "milling"/"baking"/"brewing" respectively. Base.BUILDING_PROPERTIES,
      RecruitCatalog.SPECIALIZATIONS, and SkillPanel.SKILL_DISPLAY_ORDER/
      SKILL_LABELS all restored to include the 3 refinement skills again.
    - Verified headless: instantiated Mill/Bakery/Brewery/Grain Farm from
      their catalog entries and confirmed get_skill_id() returns
      "milling"/"baking"/"brewing"/"farming" respectively.
    - Post-review fix: docs/stats.md's Farm-class description ("exported
      `input_per_tick`, `input_resource`, `sprite_tint`") dropped
      `skill_id` from the list twice now - once when the original
      unification commit removed the export without updating this prose
      line, and again here, since restoring the export didn't restore the
      line either (it's easy to miss - the field is mentioned in a
      sentence, not a table row like everything else this feature
      touched). Fixed by adding `skill_id` back to the list.

- [DONE] Implement building system
  - Citizens need to drop off building materials at building site
  - Then preferring citizens with the highest proficiency in the new construction skill the building must be contructed which requires a certain amount of labor
    - Confirming placement no longer instantiates the real building on the
      spot - it reserves the footprint and spawns a new ConstructionSite
      (scripts/construction_site.gd, scenes/workstation/ConstructionSite.tscn,
      extends Workstation - reused rather than a fresh duck-typed class
      specifically so it could plug into the existing Gale-Shapley job-
      assignment system for the labor phase with zero changes to that
      algorithm) instead, tracked in a new Base.construction_sites list.
      Resources aren't spent at placement-confirm time anymore (though
      affordability of the full cost is still checked then, matching the
      ghost's existing green/red tint) - GameState.spend() now happens
      gradually as each material actually gets delivered.
    - Materials phase: ConstructionSite.materials_needed (resource_name ->
      remaining amount, seeded from the option's cost) is drained by
      ordinary haulers. Character._find_haul_job() gained a third job kind
      alongside output-pickup/input-delivery - any site's still-needed
      resources compete on equal footing (compared by how much each would
      move, same as the existing two kinds already were) for the same pool
      of idle/assisting labor - and a new _deliver_construction_material()
      does the actual walk/spend/deliver, with the same re-check-on-arrival
      /refund-surplus safety _deliver_to_post already has (multiple haulers
      converging on one site from an identical "still needed" snapshot).
      A resource's entry is erased (not left at 0.0) once fully delivered;
      once materials_needed is empty the site emits materials_ready.
    - This is also the gate that keeps a site OUT of Base.posts (and so
      invisible to _job_posts()/_run_job_assignment()) until it's actually
      buildable - Base._on_construction_materials_ready() is the only place
      a site is ever added to posts. From there it's a completely ordinary
      job post training a new "construction" skill (added to SkillTitles.
      TITLE_SKILLS/JOB_NOUNS ("Builder"), RecruitCatalog.SPECIALIZATIONS,
      SkillPanel.SKILL_DISPLAY_ORDER/LABELS) - "preferring citizens with the
      highest proficiency" is just what _run_job_assignment's existing
      swap-on-strict-improvement matching already does for any skill, no
      special-casing needed at all.
    - Labor phase: new Character._run_construction_loop() - no haul cycle,
      no input/output buffer, just adds a flat CONSTRUCTION_LABOR_PER_TICK
      (scaled by the worker's construction skill multiplier and the
      settlement's happiness output multiplier, the same "skill/happiness
      scale output only" pattern every other work loop follows) to
      ConstructionSite.labor_completed every work_interval tick.
      labor_required is derived from the option's own cost
      (Base._labor_required_for: sum of every resource unit *
      LABOR_PER_MATERIAL_UNIT, floored at MIN_LABOR_REQUIRED) rather than
      hand-authored per building, so bigger/pricier buildings automatically
      take longer without new catalog data. Reaching it calls
      ConstructionSite.mark_complete(), emitting construction_complete.
    - Completion: Base._on_construction_complete() evicts the current
      worker, frees the site, and calls a new _materialize_building()
      helper - the exact instantiate/apply-properties/grant-capacity/
      register-as-post-or-stockpile sequence _confirm_placement() used to
      do synchronously at placement time, factored out so it can run once
      construction actually finishes instead. A fresh job-assignment pass
      follows immediately, so the just-freed builder (or anyone else) can
      pick up the newly-finished post right away.
    - Save/load: a new "construction_sites" save section
      (Base._serialize_construction_sites/_restore_construction_sites,
      same wipe-and-rebuild-from-scratch pattern _restore_placed_buildings
      already uses) persists materials_needed and labor_completed exactly,
      and shares _next_placed_id's counter/"placed_N" prefix with
      _placed_buildings - a site's save_id carries over unchanged once it
      becomes the real building, not reassigned. Base._post_by_save_id()
      now checks construction_sites too, so a citizen saved mid-labor on a
      site resumes correctly on load (and, if their materials-phase site
      had already reached materials_ready before the save, _spawn_
      construction_site() puts it straight back into posts rather than
      reverting to the materials phase).
    - Deliberately no bespoke art: every ConstructionSite reuses one
      generic scaffold-tinted sprite (iso_workstation_generic.svg)
      regardless of the target building - the in-world label communicates
      phase/progress instead ("Needs: 5 Wood" during materials, "Building
      65%" during labor). The separate "Unique art for every building" item
      elsewhere in this file is what would eventually replace this.
    - Verified headless end-to-end via a real Base scene, not mocked
      pieces: placed a Lumber Camp, confirmed wood was NOT spent yet and
      the site wasn't a job post; let the 3 starting (all-idle) citizens
      discover and actually deliver the wood for real over real time,
      confirmed the spend happened only then and the site became a job
      post; confirmed a citizen was auto-assigned to build it; fast-forwarded
      labor_completed to just under the finish line and let the next real
      tick push it over, confirming the real building appeared as a job
      post in the same position, capacity/registration applied, and the
      builder got reassigned (in this run, right back onto the finished
      Lumber Camp itself). Separately verified a full save/reload round-trip
      on a second, hand-mutated in-progress site (partial materials +
      partial labor) - confirmed both values survived exactly and its
      footprint stayed reserved. Not verified in-editor for feel (whether
      the generic scaffold art reads clearly as "under construction", label
      text legibility, timing/pacing of the two phases) - recommend a
      playtest.
    - Post-review fix, from real playtest feedback: citizens weren't
      prioritizing building at all - construction sites just sat stalled in
      the materials phase forever while everyone kept doing routine
      hauling instead, so nothing ever got far enough to open a labor slot.
      Root cause: Character._find_haul_job() picked whichever of
      output-pickup/input-delivery/construction-delivery would move the
      *largest* amount, with construction's own per-trip amount capped by
      carry capacity (~6-8 units) same as the other two - but once a
      couple of workstations are actually running, their output/input
      amounts regularly match or exceed that, so construction almost never
      won the comparison in a real (non-empty-economy) playthrough, only
      in the isolated fresh-boot-no-competition scenario the original
      headless test happened to cover. Fixed by giving construction
      delivery unconditional priority: a new _find_construction_haul_job()
      is checked first and returned immediately if it finds anything,
      before output/input hauling are even considered (which keep their
      old amount-vs-amount comparison against each other, unchanged) - a
      stalled build blocks an entire job post from ever opening, while a
      slightly-delayed output/input haul just waits for the next
      comparison, so this is a real priority difference, not just a tie-
      break tweak.
    - Verified headless: built a real, staffed Lumber Camp with its
      output_buffer set to 50 (registered in Base.posts, exactly like a
      normal placed building) alongside a construction site needing only 4
      wood, and confirmed _find_haul_job() picked the construction
      delivery over the 12x-larger output-pickup job. Separately confirmed
      the fallback path is unchanged: with no construction site pending,
      the same citizen against the same Lumber Camp still correctly picks
      the output-pickup job as before.

- [DONE] Fix citizens not dropping off at storehouse and prefering outpost hall instead
    - Root cause: WorldGrid.stockpile_spot was a single fixed Vector2, set
      once to the Outpost Hall's position in Base._ready() - every haul
      trip (Character._haul_to_stockpile/_deliver_to_post) always targeted
      it specifically, so a Storage Facility never had any effect beyond
      raising storage_capacity, no matter where it was built.
    - Replaced with WorldGrid.stockpile_spots (an array) plus
      register_stockpile()/unregister_stockpile()/nearest_stockpile(from).
      Base registers the Outpost Hall's spot once in _ready(), and now
      registers/unregisters any placed building exposing
      get_stockpile_spot() (Outpost Hall or Storage Facility - a second
      placed Outpost Hall now works as an additional stockpile too, not
      just a cost sink) at placement/load-rebuild time. StorageFacility
      gained get_stockpile_spot() (reusing its existing worker_spot
      marker). Character's two haul functions now call
      WorldGrid.nearest_stockpile(global_position) instead of reading the
      single fixed spot, so a haul trip goes to whichever registered
      stockpile is actually closest at the time.
    - Verified headless: built a Storage Facility next to the starting
      Farm (closer to it than the Outpost Hall) and confirmed
      nearest_stockpile() from the Farm's position correctly resolved to
      the new Storage Facility's spot, not the Outpost Hall's.

- [DONE] Save citizens current position with save data
    - New Character.snap_to_position(pos) kills any in-flight move tween
      and sets both _base_position and position directly - used only by
      Base._restore_characters, right after assign_to() (which already
      moves them toward their post/home and restarts their work loop),
      to override that with wherever they actually were when saved. The
      restored loop wasn't told which step it was on, so it just resumes
      naturally from this exact spot (e.g. a haul in progress continues
      from here on its next _move_to rather than restarting from the
      post). Older saves without a "position" field just keep assign_to's
      normal placement, unchanged from before this field existed.
    - Verified headless: moved a citizen to an arbitrary far-off position,
      saved, moved them elsewhere, loaded, and confirmed their position
      matched the saved one exactly.
    - Post-review fix: assign_to()'s own _move_to call (made right before
      snap_to_position() overrides it) was granting real "speed" xp for a
      move that never actually plays - since _restore_characters calls
      assign_to() for every character on every load, this handed every
      restored citizen a free chunk of speed xp per load (at least
      MIN_MOVE_DURATION's worth, more for one far from their post),
      repeatable indefinitely via F9 spam. Predates this commit (the same
      unconditional assign_to() call already existed in _restore_characters
      before position saving did) but only just noticed while reviewing
      this. Fixed by adding a grant_xp/grant_move_xp parameter threaded
      through _move_to -> assign_to, defaulting to true everywhere except
      _restore_characters's restoration call.

- [DONE] Citizens assigned to a job who are idle should help with hauling at other work sites
    - Extracted the actual pickup/delivery execution out of
      _run_hauler_loop into a new shared _execute_haul_job(job, session),
      and added _assist_haul_or_wait(session) (finds the single most
      valuable haul job across every post via the same _find_haul_job an
      idle citizen already uses, executes it if found, else falls back to
      the same IDLE_RETRY_DELAY wait as before). Wired into the two
      "blocked, nothing useful to do here right now" moments that
      previously just idled: _run_farm_loop's post-haul-trip "input still
      didn't restock" wait, and _run_lumberjack_loop's "no tree available
      in range" wait. assigned_post is untouched throughout - this is a
      one-off assist, not a reassignment, so the worker's own dedicated
      loop resumes normally afterward. _run_generic_work_loop (StoneMine
      etc.) has no equivalent blocked-wait moment to begin with (no input
      dependency), so nothing to change there.
    - Verified headless: starved a Farm worker of wood (so their input
      never restocks) while a separately-placed Stone Mine sat with a full
      output buffer, and confirmed the Farm worker hauled the mine's stone
      to the stockpile during the wait while remaining assigned to the
      Farm throughout.

- [DONE] Replace basic farm with cabbage farm
    - Cosmetic rename only: BuildingCatalog's "farm" entry's display_name
      is now "Cabbage Farm" (id stays "farm" - it's a save-persisted
      identifier, not player-facing, and renaming it would break
      BuildingCatalog.get_option("farm") lookups for old saves'
      fixed_farm_option_id). The fixed scene node's own display_name
      override in Base.tscn matches. resource_type stays "food" rather
      than becoming a new "cabbage" resource - that would ripple into
      GameState.FOOD_RESOURCES, the hunger system, HUD's Food label, and
      save compatibility for a purely cosmetic ask, so a Cabbage Farm
      still produces generically-named "food" under the hood (same
      trade-off Grain/Hops/Fruit/Potato Farms don't have, since those
      already had their own distinctly-named resource from the start).
    - Verified headless: confirmed the fixed Farm node, its BuildingCatalog
      entry, and the build menu's derived short-name button label all read
      "Cabbage Farm"/"Cabbage" consistently.

- [DONE] Levels should improve efficiency in a skill
  - ex. Same inputs -> more outputs
  - exceptions are of course speed and strength which just scale higher
    - Only one real change needed: Character._run_farm_loop's input_needed
      was `farm.input_per_tick * multiplier` (skill scaled both sides of
      the conversion, preserving the ratio - more throughput, not better
      efficiency); now it's flat `farm.input_per_tick`, unscaled, while
      output stays `output_per_tick * multiplier` as before. A higher-level
      farmer now genuinely gets more food/crops out of the same wood/
      intermediate good, not just proportionally more of both.
      Lumberjack/StoneMine already worked this way and needed no change:
      a chop removes a fixed amount from the tree regardless of skill
      (WorldTree.harvest(camp.wood_per_chop), unscaled) and the multiplier
      only affects how much of that gets credited to the buffer; simple
      gatherers have no input at all to begin with. Speed/strength are
      the stated exception and already scale movement speed/carry
      capacity directly rather than a conversion ratio - untouched.
    - Verified headless: boosted a citizen to level 50 farming (1.98x
      multiplier) and confirmed one production tick drained input_buffer
      by exactly 0.5 (the flat per-tick cost, not 0.5*1.98) while
      crediting 1.98 to output_buffer (1.0*1.98, unchanged scaling).

- [DONE] Track all types of food under the food category in the UI but have a collapsable drop down with individual food types listed as well
  - Recruiting a new citizen will now offer a choice of citizens each costing one of the types of food with the higher tier foods granting citizens with higher starting levels, the higher level citizens should cost their particular food tier as well as an equal amount of all food from lower tiers, this keeps all food types relevant throughout progression
    - First bullet (the collapsible food UI) was already covered by an
      earlier pass this session (see the "Food is still not being
      displayed properly" [DONE] entry further up) - GameState.
      FOOD_RESOURCES/get_total_food() and the HUD's collapsible Food row
      already existed by the time this item was picked back up.
    - New RecruitCatalog.FOOD_TIERS := ["cabbage", "potato", "fruit",
      "bread", "beer"] - a *separate* const from GameState.FOOD_RESOURCES
      (which is deliberately no longer an ordered priority list, see its
      own doc comment) - defines the recruit-cost/level progression
      specifically, cheapest/simplest crop to most-refined luxury good.
      generate_candidates() now shuffles FOOD_TIERS (not just NAMES) and
      assigns each of the `count` offered candidates a distinct tier where
      possible, so the 3 offered choices read as an actual cheap-vs-
      expensive spread rather than 3 near-identical draws.
    - Starting level: RecruitCatalog.STARTING_LEVEL (5, unchanged) applies
      at tier 0 (cabbage); each tier above adds TIER_LEVEL_STEP (5) more,
      so a beer-tier (index 4) candidate starts at level 25 instead of the
      old flat 5 for everyone.
    - Cost: RecruitCatalog._cost_for_tier(tier_index) charges
      RECRUIT_UNIT_COST (15, same value the old flat RECRUIT_COST used)
      of the candidate's own tier's food *and* every cheaper tier's food -
      e.g. a bread-tier (index 3) candidate costs 15 cabbage + 15 potato +
      15 fruit + 15 bread, not just 15 bread - so cabbage/potato/fruit stay
      a real, ongoing cost even once a player has Bakeries/Breweries
      running, rather than being obsoleted the moment better food exists.
      RecruitCatalog.RECRUIT_COST (the old flat constant) is gone;
      Base._on_candidate_selected now reads candidate["cost"] (per-
      candidate, not global) for both the affordability check and the
      spend call.
    - RecruitPanel's candidate buttons now show a second line with the
      actual per-candidate cost (e.g. "15 Cabbage + 15 Potato + 15 Fruit"),
      built from the cost dict's insertion order (already low-tier-first,
      so no separate sorting needed) and reusing HUD.FOOD_BREAKDOWN_LABELS
      so "beer" reads as "Ale" here too, matching the Food panel. The
      panel was widened (280->360px offset_right, -220->-280px offset_top)
      and buttons given `autowrap_mode` since a 5-food-type cost line runs
      long enough to wrap even in the widened panel.
    - Verified headless: generate_candidates(3) produced 3 candidates with
      3 distinct tiers, each with a cost dict exactly matching the
      cumulative-tier formula and a level exactly matching
      STARTING_LEVEL + tier_index*TIER_LEVEL_STEP; specifically confirmed
      a cabbage-tier candidate costs only {cabbage: 15} at level 5
      (matching the old pre-tier behavior exactly) and a beer-tier
      candidate costs all 5 food types at level 25. Full integration
      verified through a live Base scene: recruiting a beer-tier candidate
      with 100 of every food type correctly spent 15 from each of the 5
      down to 85 and incremented population_count; a follow-up recruit
      attempt with insufficient beer (5, need 15) was correctly denied
      with population_count unchanged. Not verified in-editor for feel
      (whether the two-line button text/word-wrap reads cleanly at the
      widened panel size) - recommend a playtest.

- [DONE] Allow the game window to scale with different resolutions
  - Don't stretch the image
    - Added a [display] section to project.godot (base 1280x720,
      window/size/resizable=true, previously using Godot's own bare
      defaults with no explicit stretch settings at all). stretch/
      mode="canvas_items" scales the whole 2D scene and UI together as
      one image rather than a naive post-hoc stretch, so text/sprites
      stay crisp and correctly proportioned at any window size.
      stretch/aspect="expand" means a window with a different aspect
      ratio than the base just reveals more world/UI margin at the edges
      (rather than letterboxing black bars or cropping/distorting) - a
      good fit for a free-scrolling RTS camera that doesn't care how much
      of the map is visible at once.
    - Not verified visually (this environment has no display/GUI to
      actually resize a window in) - recommend an in-editor check:
      resize the game window to a few different sizes/aspect ratios and
      confirm nothing looks stretched or distorted, and that the HUD's
      anchored corners still read correctly at each size.

- [DONE] Implement floating then fading text indicating when a material is gathered, how much was gathered, and how much xp was gained
  - This should be very minimal so as not to clutter the screen too much
  - Satisfying feedback for steady progression
    - New scripts/gather_feedback.gd (GatherFeedback, extends Label).
      GatherFeedback.spawn(parent, local_position, resource_type, amount,
      xp) builds a small "+1.2 Food  +4 xp" Label (white text, black
      outline for readability against any background), positions it just
      above the spawn point, then tweens it upward 28px while fading its
      alpha to 0 over 1 second, and queue_free()s itself once that
      finishes. z_index=100 keeps it drawing on top of every normal
      y-sorted child regardless of position, the same trick CLAUDE.md
      documents for IsoGround always rendering behind everything.
    - Wired into all three of Character's production-tick sites (the
      generic work loop, the Farm-class loop, and the Lumberjack chop
      loop) - each already computed a produced `amount` inline, so each
      site now stores that in a local var and calls
      _show_gather_feedback(resource_type, amount, XP_PER_GATHER) right
      after gather_sound.play()/_gain_skill_xp(). _show_gather_feedback
      spawns the label as a child of get_parent() (Base, since every
      Character is a direct child of it) at the character's own
      `position`, which is already in Base's local coordinate space -
      no global/local conversion needed.
    - Verified headless: placed a Stone Mine, assigned a worker, waited
      for its first production tick (work_interval=1.5s) and confirmed a
      GatherFeedback child appeared on Base reading "+0.5 Stone  +4 xp"
      (matching Stone Mine's output_per_tick/skill multiplier and
      XP_PER_GATHER); waited past the 1-second fade (but before the next
      1.5s tick, to avoid the next tick's own label confusing the check)
      and confirmed it was gone (queue_free'd).

- [DONE] House upgrade with stone to increase capacity
  - House converted from a plain Node2D to an Area2D (mirroring
    OutpostHall's click pattern - same CollisionShape2D size/position,
    since both are 2x2 footprints sharing the same iso art proportions),
    emitting a `clicked` signal on left-click.
    - House.UPGRADE_COST (15 stone) and UPGRADE_CAPACITY_BONUS (+2) are
      constants on House; House.mark_upgraded() only flips the `upgraded`
      flag and refreshes the label (to "House (Upgraded)") - spending the
      cost and granting the capacity bonus live in
      Base._on_house_clicked, since only Base/GameState know whether the
      player can currently afford it. A second click on an already-
      upgraded House is denied with a HUD message rather than double-
      spending.
    - Base._wire_house_clicks (parallel to the existing
      _wire_farm_clicks) connects the signal at both of a House's two
      construction paths - fresh placement (_confirm_placement) and
      save-file rebuild (_restore_placed_buildings).
    - Persistence: _serialize_placed_buildings now includes an
      "upgraded": true entry for any House that's been upgraded (omitted
      otherwise, so old saves round-trip with no field at all);
      _restore_placed_buildings calls building.mark_upgraded() when that
      flag is set. The actual capacity bonus itself doesn't need
      separate restoration - GameState.population_capacity is already
      saved/loaded as a raw total (see Base._restore_placed_buildings'
      pre-existing doc comment on why placed buildings never re-grant
      capacity on load).
    - Verified headless: placed a House, confirmed an upgrade attempt
      with 0 stone was denied (upgraded stays false, capacity
      unchanged); granted 20 stone and confirmed upgrading succeeded
      (upgraded=true, label reads "House (Upgraded)", population
      capacity +2, stone spent down to 5); confirmed a second upgrade
      attempt on the same House was denied and capacity didn't change
      again; saved and reloaded and confirmed the reloaded House
      instance still read upgraded=true with the correct label and the
      capacity bonus intact.

- [DONE] Replace placeholder farm art with recolored cabagge farm art
  - CropStation.tscn (the shared scene behind every Alternative Crop Type
    - Grain Farm, Mill, Bakery, Hops Farm, Brewery, Fruit Orchard, Potato
    Farm) was baking in `iso_workstation_generic.svg` as its own default
    sprite, undersized/mis-proportioned for a 2x2 footprint - since none
    of these buildings' resource_type values ("grain", "flour", "bread",
    etc.) match a Workstation.RESOURCE_VISUALS key, refresh_visual()
    always fell through to that baked default rather than ever picking up
    the real Cabbage Farm art. Each one's existing `sprite_tint` (already
    defined per catalog entry - golden for Grain Farm, green for Hops
    Farm, etc.) was recoloring the wrong, tiny placeholder icon instead of
    an actual farm building.
  - Fixed by pointing CropStation.tscn's Sprite2D at
    `iso_workstation_farm.svg` with the same offset/centered
    Workstation.RESOURCE_VISUALS already uses for "food" (Vector2(-138,
    -106), centered=false), and widening its CollisionShape2D from
    140x70 to 240x120 to actually cover a 2x2 footprint instead of
    something closer to 1x1.
  - Farm.tscn (literal "Cabbage Farm") got the identical fix, even though
    its own resource_type ("food") already resolves through
    RESOURCE_VISUALS at runtime and so already displayed correctly -
    otherwise retooling a placed Cabbage Farm into any Alternative Crop
    Type via the crop panel (a supported, documented interaction) would
    have fallen back to Farm.tscn's own stale baked default the moment
    refresh_visual() couldn't find the new resource_type in
    RESOURCE_VISUALS, undoing this same fix for that one building.
  - Verified headless (comparing resolved Sprite2D.texture.resource_path,
    since there's no display to eyeball the art in this environment -
    same caveat as the earlier resizable-window item; recommend an
    in-editor check to confirm it actually looks right tinted): a placed
    Cabbage Farm and a placed Grain Farm both resolved to
    `res://art/iso_workstation_farm.svg` (Grain Farm's sprite modulate
    confirmed as its catalog-defined golden tint); retooling the Cabbage
    Farm into a Mill via _apply_crop_option kept that same texture
    rather than reverting to the old placeholder.

- [DONE] Implement level 'titles' a citizen can earn for reaching certain levels in a skill
  - Only one title can be active at a time, the highest one they have earned. If multiple of the same level choose current job title
  - Shown below their name
  - Apprentice -> Journeyman -> Master -> Grandmaster -> Legendary
    - Precedes the job title ex. Legendary Farmer, Master Lumberjack
    - New scripts/skill_titles.gd (SkillTitles, static RefCounted utility
      like SkillCurve/BuildingCatalog). TITLE_SKILLS restricts titles to
      the six job skills (farming/lumberjacking/mining/milling/baking/
      brewing) - speed/strength are excluded since they train passively
      just by existing rather than representing a trade, so there's no
      natural job noun to pair a tier word with. TIER_THRESHOLDS is
      [1,"Apprentice"], [25,"Journeyman"], [50,"Master"],
      [75,"Grandmaster"], [99,"Legendary"] - 99 (SkillCurve.MAX_LEVEL) is
      its own top tier rather than folded into Grandmaster, so
      "Legendary" stays a rare payoff for actually maxing a skill out.
    - SkillTitles.get_title(data, current_skill_id) finds the job skill
      with this citizen's highest level, ties broken toward
      current_skill_id if it's one of the tied skills (otherwise stable,
      first-in-TITLE_SKILLS-order), and returns "<tier> <job noun>" (e.g.
      "Master Lumberjack"). A citizen with nothing trained yet (every
      skill at level 1) still gets a baseline "Apprentice <first skill>"
      title rather than going title-less.
    - Wired into Character._update_label(), which already computes
      `skill_id` (the current assignment's trained skill, or "" if
      unassigned/hauling) to use as the tiebreak - the in-world label
      gained a new middle line: "<name>\n<title>\n(<status>)".
    - Verified headless (calling SkillTitles.get_title directly against
      fresh CharacterData resources, not needing a live scene): a fresh
      character reads "Apprentice Farmer"; level 50 farming reads "Master
      Farmer"; level 99 lumberjacking (with farming still at 50) reads
      "Legendary Lumberjack"; two skills tied at level 50 correctly
      picked the current-job skill's noun when given a matching
      current_skill_id, and fell back to first-in-order (farming) when
      given an empty one; baking's tier word was checked at every
      threshold boundary (24/25/74/75/99) and landed exactly on
      Apprentice/Journeyman/Master/Grandmaster/Legendary respectively;
      and a live Character's label.text was confirmed to read "Aldric |
      Master Farmer | (Hauling)" after setting their farming xp to
      level 50.

- [DONE] Remove starting buildings other than outpost hall
  - Make sure starting resources are enough to build a lumber camp
    - Removed the fixed $Farm (Cabbage Farm) and $Woodpile (Lumber Camp)
      nodes from Base.tscn entirely - only the Outpost Hall starts built
      now. Starting resources (10 food, 10 wood) already comfortably
      cover a Lumber Camp's 5 wood cost, so no balance change needed
      there. Removed everything that specifically depended on those two
      fixed nodes existing: Base.posts/_reserve_existing_footprint
      registration, _wire_farm_clicks($Farm), the "farm"/"woodpile"
      special cases in _post_by_save_id (an old save referencing either
      now just resolves to null/unassigned rather than crashing on a
      missing node), and the whole _fixed_farm_option_id/
      "fixed_farm_option_id" save-field mechanism from the crop-selection
      feature (every Farm-family building is a normal placed one now, so
      _on_crop_selected's _placed_buildings-entry-update path covers all
      of them uniformly - no more special-casing needed). _scatter_
      initial_trees() now scatters around the Outpost Hall instead of the
      (no longer existing) starting Lumber Camp.
    - Verified headless: confirmed posts only contains the Outpost Hall on
      a fresh boot, starting wood affords a Lumber Camp, initial trees
      still scatter (12, now around the Outpost Hall), and all 3 starting
      citizens default to hauling with no crash. Separately verified a
      full save/load round-trip with a player-built Lumber Camp (place it,
      assign a worker, save, load, confirm both the post and the
      assignment survived) to make sure removing the fixed-building
      machinery didn't regress the now-only-path for Farm-family
      buildings. Not verified in-editor for feel/balance (whether the
      early game reads as too slow now that literally everything must be
      built from scratch) - recommend a playtest.

- [DONE] Get rid of manual assignment system
  - Each pawn should automatically take a job matching whatever their highest stat is
  - Citizen who can't find a job will become haulers until a job opens up for their skill set
  - If a citizen has a job but a new citizen arrives that has a higher skill in that job the previous job holder should swap for the better qualified citizen
    - Scope decision (asked the user first): the spec only mentioned job
      posts and hauling, not Wall Segments or the Outpost Hall/Storage
      Facility's old explicit-hauler-assignment option. The user chose to
      remove Wall Segments entirely rather than keep them manual or leave
      them unhandled - turned out to be pure upside, since a repo-wide
      grep found WallSegment/Gate/WallCornerPeak/WallCornerPoint were
      100% dead code already (never instantiated by Base.tscn or any
      BuildingCatalog entry, every reference to "WallSegment" anywhere
      in scripts/ was in a doc comment, not real code) - removed
      scripts/wall_segment.gd, scripts/gate.gd, scenes/wall/*.tscn, and
      their 4 art SVGs entirely. (scripts/perimeter_wall.gd and
      scenes/world/PerimeterWall.tscn are a separate, unrelated dead
      system CLAUDE.md already documents as intentionally kept as
      history - left alone.)
    - All manual assignment removed: Character no longer supports
      dragging at all (start_drag/update_drag/end_drag/cancel_drag/
      drag_started deleted); clicking a citizen now only selects them and
      opens their skill panel (view-only) - clicking the same citizen
      again just deselects, and there's no more "click a post while a
      citizen is selected" or "drag onto a post" path anywhere. Base's
      _post_at/_post_has_room/_assign_selected_to/_on_character_drag_started/
      _handle_drag_input/_end_character_drag/_cancel_character_drag are
      all gone. Outpost Hall's click now always opens the recruit panel
      (no more "assign selected citizen as hauler" branch); a Farm-family
      click now always opens the crop panel the same way. OutpostHall/
      StorageFacility lost their add_worker/remove_worker/active_workers/
      max_workers/get_worker_spot entirely (nothing calls them anymore -
      a citizen is never explicitly "assigned" to either again) and their
      labels reverted to plain names, since a permanently-stuck "0/10"
      count would've been worse than no count at all.
    - New Base._run_job_assignment() (called on every citizen recruited/
      departed, job post built/disabled/re-enabled, and game load/boot)
      implements the matching as citizen-proposing Gale-Shapley deferred
      acceptance generalized to multi-worker posts: each citizen ranks
      the 6 job skills by their own level (new
      SkillTitles.skill_preference_order, sharing TITLE_SKILLS/JOB_NOUNS
      with the skill-titles feature), then repeatedly proposes to their
      best not-yet-rejected skill's shared post pool (posts of the same
      skill_id pool their capacity together); a pool under capacity
      accepts immediately, a full pool evicts its weakest occupant only
      on a *strict* level improvement (a tie changes nothing - no
      thrashing between equally-qualified citizens) - this eviction step
      *is* "swap for the better-qualified citizen," not a separate code
      path. Whoever exhausts every preference without landing a post
      stays unassigned, which Character.assign_to(null) already turns
      into hauling automatically.
    - Caught and fixed a real design bug before it ever shipped: an
      earlier version of this grouped each citizen into a single
      precomputed "best skill" bucket before matching. Every fresh
      citizen ties at level 1 across all six skills, so a fixed tie-break
      put literally everyone's "best skill" at TITLE_SKILLS[0]
      ("farming") - if the player's first building happened to be a
      Lumber Camp rather than a Farm, nobody would ever be eligible to
      work it. Fixed by having skill_preference_order return a citizen's
      *full* ranked preference list rather than a single skill, so the
      matching algorithm falls through to a citizen's 2nd/3rd/etc. choice
      when their 1st has no room (or doesn't exist yet) - caught by a
      headless test that specifically builds a Lumber Camp before any
      Farm exists.
    - Bonus feature (explicitly requested mid-task, not in the original
      backlog line): any placed job post can now be right-clicked to
      toggle it disabled/re-enabled (Workstation.disabled, gated by a new
      _on_workstation_input_event handler - deliberately a different
      method name than Farm's own _on_input_event override, since
      connecting a same-named method from Workstation's _ready() would
      resolve virtually to Farm's override instead and silently skip
      this handler on every Farm-family post). Disabling immediately
      evicts the current worker (Base._on_post_disabled_changed) and
      excludes the post from _run_job_assignment (0 effective capacity)
      until re-enabled, when a fresh assignment pass gives it a chance to
      be staffed again right away. Shows "(Disabled)" in the post's label
      and dims its sprite (DISABLED_TINT multiplied onto sprite_tint, so
      a crop's own color is dimmed rather than replaced). Persisted
      through save/load as a new "disabled" flag on placed-building save
      entries, same pattern as House's "upgraded" flag.
    - Verified headless end to end: fresh boot leaves all 3 starting
      citizens hauling (no posts exist yet); building a Lumber Camp
      *before* any Farm still gets it staffed (the tie-break bug fix,
      above); building a Farm afterward staffs it too, leaving the third
      citizen hauling; recruiting a level-50-farming citizen correctly
      displaces the existing (untrained) farm worker, who becomes a
      hauler; disabling the Lumber Camp evicts its worker and its label
      reads "Lumber Camp (Disabled) 0/1"; it stays empty through a
      repeat assignment pass while still disabled; re-enabling gets it
      restaffed; and a full save/reload round-trip correctly restored
      the disabled flag with 0 workers on the reloaded node.

- [DONE] Citizens consume too much food, the vast majority of the economy has to be devoted to farming to sustain the population, lets reduce consumption slightly
    - GameState.FOOD_PER_CITIZEN dropped 0.3 -> 0.2 per citizen per second (a
      ~33% cut) - a first-pass tuning number, not derived from a formula,
      same spirit as every other first-pass balance number in this project.
    - Verified headless: confirmed the constant reads 0.2 and is strictly
      below the old 0.3.

- [DONE] Food is still not being displayed properly. There should be a food category with an aggregate food amount displayed, then below that and indented should be each food type listed individually in a collapsable menu.
    - Root cause of the "cabbage patches harvesting generic food" bug: the
      Cabbage Farm's BuildingCatalog entry (id "farm") set resource_type to
      the generic literal "food" rather than a real "cabbage" resource -
      there was never an actual "cabbage" resource in GameState.resources
      at all, just a "food" one every Farm-family building's early history
      happened to share. Fixed by renaming that one catalog entry's
      resource_type to "cabbage", moving GameState.DEFAULT_RESOURCES'
      starting 10-units entry from "food" to "cabbage" (Wood stays the same
      10), and repointing Workstation.RESOURCE_VISUALS' "food" key (the
      farm-art sprite lookup) to "cabbage" so the Cabbage Farm's visual
      still resolves correctly. Since GatherFeedback's floating text already
      reads directly off resource_type (see the earlier gather-feedback
      feature), this alone fixes the reported "+1.2 Food" text into
      "+1.2 Cabbage" with no separate change needed. RecruitCatalog.
      RECRUIT_COST and its RecruitPanel title text moved from "food" to
      "cabbage" too (cabbage being the base/starting food type), and
      Base's happiness "does the settlement have any food at all" check now
      reads the new GameState.get_total_food() aggregate instead of a
      literal resources["food"] lookup that no longer exists.
    - GameState.FOOD_RESOURCES (the hunger-eligible list) reordered to
      cabbage/potato/fruit/bread/beer and a new get_total_food() sums it -
      used by both the HUD's aggregate row and Base's happiness check.
      Wheat (the "grain" resource) and flour are tracked in the HUD
      breakdown (see below) but deliberately excluded from FOOD_RESOURCES/
      get_total_food(), matching "inedible on their own... but should still
      be tracked."
    - HUD.tscn restructured: the old fixed-offset row-per-label layout
      became a VBoxContainer ("Rows") so inserting new rows doesn't require
      manually recomputing every following label's pixel offset. The old
      single FoodLabel became FoodButton (a flat, click-to-toggle Button)
      plus a FoodBreakdown VBoxContainer (initially hidden) that hud.gd
      populates at _ready() with one Label per HUD.FOOD_BREAKDOWN_ORDER
      entry (cabbage/potato/fruit/bread/beer, then grain/flour - 7 total),
      each showing "<Name>: amount/cap (+rate/min)" the same format the
      existing Wood/Stone rows already use. Clicking FoodButton toggles
      food_breakdown.visible and flips a "> Food: N" / "v Food: N" arrow
      prefix on the aggregate line; the surrounding Control's height is
      sized generously for the fully-expanded state up front (544px) rather
      than dynamically resized on toggle, so a collapsed panel just leaves
      empty panel space below the Build/Menu buttons - simpler and safer
      than live-resizing an anchored Control, and this compact corner panel
      has plenty of unused vertical room at the base 1280x720 resolution.
      "Ale" and "Wheat" are display-only relabels of the beer/grain
      resource keys (kept internally as-is, since beer/grain are already
      threaded through Brewery/Mill/BuildingCatalog/save data) - only the
      HUD's FOOD_BREAKDOWN_LABELS dict and the RecruitPanel/docs prose
      needed updating for the rename.
    - Mid-implementation correction (explicit follow-up request): the
      initial pass kept the old sequential priority-order consumption
      (drain cabbage fully, then potato, then fruit, etc.) just reordered
      to put cabbage first - the ask was actually for an *even split*
      across every stocked food type every tick, not a priority order at
      all. Rewrote GameState._consume_food as an iterative water-filling
      split: divide the remaining hunger need evenly across every
      currently-stocked FOOD_RESOURCES entry, and for any type that runs
      out partway through its even share, redistribute the shortfall
      evenly across whatever's still stocked in a follow-up round (bounded
      by FOOD_RESOURCES.size() rounds, since a resource can only drop out
      of the candidate pool once). FOOD_RESOURCES' list order is now just a
      stable iteration/display order, not a priority - updated its doc
      comment (and mechanics.md/stats.md) accordingly.
    - Verified headless: GameState.resources booted with "cabbage": 10.0 and
      no "food" key at all; BuildingCatalog.get_option("farm")["resource_type"]
      reads "cabbage"; a live Farm node with resource_type "cabbage"
      resolved its sprite to the real Cabbage Farm art via RESOURCE_VISUALS
      (not a fallback/default texture); the HUD's FoodButton read
      "> Food: 10" collapsed and correctly flipped to "v Food: 10" with all
      7 breakdown rows visible ("Cabbage: 10/30", "Wheat: 0/30", etc.) on
      toggle; and a simulated legacy save containing an orphaned "food" key
      loaded without crashing and correctly did NOT count toward
      get_total_food(). Separately verified _consume_food's even-split
      redistribution directly: 5 stocked types each lost an equal 1/5
      share of a flat need; a type reduced to near-empty had its shortfall
      correctly redistributed evenly across the other 4 in the same call;
      a total-need-exceeds-total-stock case drained everything to exactly
      0 with no error/negative values; and a single-stocked-type case
      correctly consumed only up to what was available, matching the old
      behavior's floor-at-0 guarantee. Not verified in-editor for feel
      (whether the collapsed/expanded Food row reads clearly at 260px
      panel width, whether the extra blank space below the buttons when
      collapsed looks intentional or like a layout bug) - recommend a
      playtest.

- [DONE] Stone brick production chain
  - Stone mine -> Brickmaker
  - Stone bricks used for upgraded houses, brewery, mill, and other advanced buildings
    - Scope decisions confirmed with the user before implementing: (1) a new
      "masonry" skill rather than reusing "mining" (matches the existing
      precedent of Mill/Bakery/Brewery each getting their own trade skill
      separate from their raw-gathering source); (2) Brickmaker is a
      dedicated, single-recipe building, NOT part of the Farm-family
      crop-selection retool system (clicking a Cabbage Farm can't retool it
      into a Brickmaker or vice versa); (3) also retrofit brick into
      House's upgrade cost and Mill/Brewery's build costs now, not deferred.
    - New `Brickmaker` (scripts/brickmaker.gd, scenes/workstation/
      Brickmaker.tscn, 1x1, reusing StoneMine's generic-sprite sizing since
      no bespoke art exists yet - see the still-open "Unique art for every
      building" item) `extends Workstation`, NOT `Farm`. It still reuses
      `Character._run_farm_loop` (the same input-buffer/output-buffer/
      haul-trip converter loop every Farm-family building already shares)
      by structural typing alone: `input_resource`/`input_per_tick` (and
      `get_input_resource()`'s implementation) were moved from `Farm` up
      onto `Workstation` itself specifically so a non-Farm-family converter
      could share the loop without extending `Farm`. Not extending `Farm`
      is also what keeps Brickmaker out of the crop-selection panel with
      zero extra guarding needed - `Base._wire_farm_clicks` only wires
      that panel to `is Farm` nodes, and Brickmaker has no `clicked` signal
      at all. `_start_work`'s dispatch became `if post is Farm or post is
      Brickmaker: _run_farm_loop(...)`. One real behavioral risk this
      generalization introduced and had to guard against: `_run_farm_loop`
      already applies `Character.WATER_FARM_OUTPUT_BONUS` (a well-access
      output bonus, previously implicitly Farm-only since only Farm used
      the loop) - broadening the loop to a second class would have silently
      given Brickmaker the same bonus too (a well boosting brick
      production makes no sense), so that line is now explicitly gated on
      `post is Farm` rather than being unconditional.
    - BuildingCatalog gained a "brickmaker" entry (1x1, 8 wood + 6 stone,
      stone -> brick at a 1:1 ratio matching every other refinement
      building's ratio, e.g. Mill's grain -> flour). New "brick" resource
      added to GameState.DEFAULT_RESOURCES (starts at 0, not food, not in
      FOOD_RESOURCES). New "masonry" skill added everywhere a job skill
      needs to be known: SkillTitles.TITLE_SKILLS/JOB_NOUNS ("Mason"),
      RecruitCatalog.SPECIALIZATIONS, SkillPanel.SKILL_DISPLAY_ORDER/
      SKILL_LABELS - Base._run_job_assignment's existing Gale-Shapley
      matching and _job_posts()'s TITLE_SKILLS filter both pick Brickmaker
      up automatically with no special-casing, same as every prior skill
      addition in this project.
    - Cost retrofits (mid-task correction from the user: House's upgrade
      specifically should drop raw stone entirely, not just add brick
      alongside it - "a stone house should be built from worked brick, not
      the raw ore"): House.UPGRADE_COST changed from {"stone": 15.0} to
      {"brick": 10.0, "wood": 15.0} (stone removed, not just supplemented).
      Mill's cost gained "brick": 6.0 (unchanged wood: 8.0). Brewery's cost
      gained "brick": 6.0 alongside its existing wood/stone cost (stone
      kept here, unlike House, since the ask was specifically about the
      *upgraded house* representing worked stone - Brewery/Mill's stone
      cost isn't being reinterpreted the same way). Bakery/Well/Storage
      Facility were left untouched - not named in the backlog item.
    - Verified headless via a real, non-mocked run: `flatpak run
      org.godotengine.Godot --headless --path .` is how Godot actually
      runs in this environment (it's a Flatpak install, not a `godot4`/
      `godot` PATH binary - see the project memory entry on this). A
      first attempt drove the test via `-s <script>` (overriding the
      SceneTree main loop) but that turned out to break global-identifier
      resolution for the `GameState`/`WorldGrid`/`SaveManager` autoloads
      for *every* script including the project's own base.gd/character.gd
      (not just the throwaway test script) - Base.tscn's script failed to
      compile under that mode and every property access on it then failed
      too. Worked around by temporarily pointing project.godot's
      `run/main_scene` at Base.tscn directly and adding a throwaway
      DebugVerify autoload to drive the test instead, letting the engine's
      normal boot sequence (which does register autoloads correctly)
      handle everything - both reverted afterward, confirmed via `git
      status` showing zero net diff on project.godot. Also needed a one-time
      `--headless --path . --import` pass first so the new Brickmaker
      global class actually got registered (a stale `.godot/
      global_script_class_cache.cfg` predating this session's new files
      otherwise makes `is Brickmaker`/bare `Brickmaker` references fail to
      resolve even under normal boot).
    - The real run: instantiated a live Base scene (real starting citizens,
      real WorldGrid/stockpile, nothing mocked), added a Stone Mine +
      Brickmaker directly as children (bypassing the placement/construction
      UI flow to save time, matching how earlier single-tick isolation
      tests in this file already do), seeded 100 stone/100 wood, and ran
      `Base._run_job_assignment()` for real - confirmed Aldric auto-matched
      to the Stone Mine (mining) and Brenna to the Brickmaker (masonry),
      leaving Cass hauling, exactly the expected Gale-Shapley outcome for 3
      citizens and 2 posts. Ran 45 real seconds: confirmed `brick` rose
      from 0 to 18 in the shared resource pool, `stone` was correctly drawn
      down while `wood` (untouched by this chain) stayed exactly 100, and
      Brickmaker's own input_buffer/output_buffer both read sane non-
      negative values throughout with zero errors printed. Also directly
      confirmed `Brickmaker.get_skill_id()`, `get_input_resource()`
      (empty before catalog application, "stone" after - proving
      `_apply_option_properties`'s generic `node.set(key, ...)` genuinely
      works on the new class with no special-casing), House/Mill/Brewery's
      updated cost dicts, and SkillTitles' masonry entries all read exactly
      as configured. mining/masonry skill levels for all 3 citizens stayed
      at 1 after 45s, which is expected (not a bug) - matches the earlier-
      verified "speed took 90 real seconds to reach level 2" precedent for
      flat per-action xp on a RuneScape-style curve, and much of the 45s
      window here was spent on haul-trip travel rather than production
      ticks specifically. Not verified in-editor for feel (Brickmaker's
      generic-sprite visual at 1x1 scale, whether the new "masonry" skill
      panel row/recruit specialization reads correctly) - recommend a
      playtest.

- [DONE] Make early game easier by increasing starting food so happiness doesn't tank immediately
    - GameState.DEFAULT_RESOURCES' starting cabbage raised 10 -> 30 (matches
      BASE_STORAGE_CAPACITY exactly, so the HUD's per-resource "amount/cap"
      breakdown row never reads as overflowed at boot). At 3 starting
      citizens * FOOD_PER_CITIZEN (0.2/citizen/sec, see the earlier
      consumption-tuning item), the old 10-cabbage start ran out in ~17
      real seconds - GameState.get_total_food() hit 0 almost immediately,
      tripping Base's happiness system into -HAPPINESS_STARVING_PENALTY
      territory before the player had any realistic chance to place, staff,
      and haul-supply a first farm. 30 cabbage stretches that to ~50
      seconds - still not infinite (starvation is still a real, intended
      pressure once it hits), but enough of a window to get a Cabbage Farm
      built and delivering food before the penalty starts accumulating.
      Starting wood (10) is untouched - it already comfortably covers a
      Lumber Camp's 5-wood cost and wasn't the reported problem.
    - No other balance numbers changed - this is deliberately scoped to
      the literal ask (more starting food), not a broader consumption-rate
      or happiness-curve pass; FOOD_PER_CITIZEN was already tuned down in
      an earlier item and is left alone here.
    - Verified headless (via `flatpak run org.godotengine.Godot --headless
      --path . -s <script>` - Godot here is a Flatpak install, not a PATH
      binary, so it needs invoking that way): confirmed
      GameState.resources["cabbage"] reads exactly 30.0 at boot (matching
      storage_capacity exactly, so the HUD's "30/30" breakdown row won't
      read as overflowed), and confirmed
      get_total_food()/(FOOD_PER_CITIZEN*population_count) computes exactly
      50.0 seconds - matching the hand-computed expectation and roughly 3x
      the old ~16.7s window. Not verified in-editor for feel (whether 50s
      actually reads as enough breathing room in practice) - recommend a
      playtest.

- [DONE] Remove floating info text from citizens (except for name, title, and floating xp and gathered item drops)
    - Character._update_label() dropped its third line entirely - the old
      "(Hauling)" / "(Lumber Camp · Lv 3)" status line (assigned post's
      display_name, or "Hauling" if unassigned, plus a " · Lv N" suffix
      when a skill applied) is gone. Label.text is now just "%s\n%s" %
      [character_name, title] instead of the old 3-line "%s\n%s\n(%s)".
      That status info isn't lost from the game, just no longer shown
      floating in-world - it's still fully visible via the (unchanged)
      skill panel on click, which already lists every skill level.
      GatherFeedback (the separate "+1.2 Cabbage +4 xp" floating text
      spawned per production tick) was untouched, matching the "keep
      floating xp and gathered item drops" carve-out.
    - Character.tscn's Label rect shrank from a 3-line box (offset_top
      -110, offset_bottom -84, 26px tall) to a 2-line one (offset_top
      -108, offset_bottom -84, 24px tall - matching Workstation's own
      2-line "Name\nfilled/max" label sizing convention) so the tighter
      content doesn't leave an oversized gap above the character's head.
    - Verified headless via a real Base scene: called _update_label()
      directly on a live starting citizen and confirmed label.text reads
      exactly "Aldric\nApprentice Farmer" (2 lines, split("\n").size()==2),
      with no status/assignment text present.

- [DONE] Brewery, mill, and bakery need to be dissasociated from the farm building class, they are specialist workshops that need their own art and identity
    - Two scope decisions confirmed with the user before implementing:
      (1) one shared new `Workshop` class (scripts/workshop.gd) rather than
      three separate near-identical classes - like `Farm`, `skill_id` stays
      a per-catalog-entry export since the class covers three distinct
      trades (milling/baking/brewing), rather than being hardcoded the way
      Brickmaker's single fixed "masonry" is; (2) one new shared 2x2
      placeholder art asset for all three (not a full bespoke pass per
      building, which is the separate still-open "Unique art for every
      building" item, and not leaving them on the old farm art either).
    - `Workshop extends Workstation`, NOT `Farm` - exactly the same
      relationship to `Character._run_farm_loop` Brickmaker already
      established (structural typing via input_per_tick/input_resource
      living on `Workstation` itself, no `clicked` signal at all). Not
      extending `Farm` and having no `clicked` signal is what keeps a
      building out of the Farm-family crop-selection retool panel with
      zero extra guarding - `Base._wire_farm_clicks` only wires that panel
      to `is Farm` nodes, and `BuildingCatalog.FARM_FAMILY_SCENE_PATHS`
      only lists `Farm.tscn`/`CropStation.tscn`, not the new
      `Workshop.tscn`. `_start_work`'s dispatch became `if post is Farm or
      post is Brickmaker or post is Workshop: _run_farm_loop(...)`.
    - Real behavioral change this causes, not just a code reorganization:
      Mill/Bakery/Brewery used to be real `Farm` instances, so they
      silently received `Character.WATER_FARM_OUTPUT_BONUS` (the
      well-access output bonus) same as any crop farm. `_run_farm_loop`'s
      water-bonus line is gated on `post is Farm` specifically (added
      earlier this session for Brickmaker's benefit) - now that Mill/
      Bakery/Brewery are `Workshop` instances instead, they no longer
      qualify and lose that bonus. Accepted as the correct consequence of
      "these are specialist workshops, not farms," not a regression to
      fix.
      - BuildingCatalog: Mill/Bakery/Brewery's "scene" changed from
      `CropStation.tscn` to the new `Workshop.tscn`; all other fields
      (resource_type/input_resource/input_per_tick/output_per_tick/
      skill_id/sprite_tint/cost, including this session's earlier brick
      cost additions) are untouched. `farm_family_options()`/
      `FARM_FAMILY_SCENE_PATHS` needed no code change at all - excluding
      Mill/Bakery/Brewery from the retool panel fell out automatically
      from them no longer being on a Farm-family scene path, the same
      "generalizes for free" pattern this whole session has repeated.
      Grain Farm/Hops Farm/Fruit Orchard/Potato Farm (the actual raw crops)
      are untouched and stay on `CropStation.tscn`/in the retool group.
    - New art: `art/iso_workstation_workshop.svg`, a 2x2 flat-shed
      silhouette (stone-toned walls, a low single-ridge shed roof, a
      chimney with a puff of smoke, and stacked barrels out front) built
      from the same proven wall/roof coordinate skeleton `iso_house.svg`
      already uses (front-corner-down footprint, left face lighter/right
      face darker on both walls and roof, matching CLAUDE.md's dimetric
      lighting rules) but with a lower/flatter roof and no door/windows -
      deliberately distinct from both the Cabbage Farm's flat tilled-soil
      plot and the House's cozy peaked roof, reading as a generic
      "industrious workshop" rather than either. All three buildings share
      this one asset, distinguished only by their existing per-catalog
      sprite_tint (same "shared silhouette, different tint" pattern
      already used elsewhere in this project, e.g. StoneMine/Brickmaker/
      ConstructionSite's shared generic sprite) - a real bespoke silhouette
      per building is explicitly deferred to the separate "Unique art for
      every building" item, not attempted here.
      - Workshop.tscn's sprite offset (-132,-153) was derived, not guessed:
      confirmed both `CropStation.tscn` (offset -138,-106, farm-art front
      vertex at SVG-local 138,138) and `House.tscn` (offset -132,-203,
      house-art front vertex at SVG-local 132,235) both resolve to the
      exact same node-local anchor point (0, 32) via `offset + front_vertex
      = anchor`, then solved that same equation for the new art's own
      front vertex (132,185) to land on that identical anchor, so Workshop
      sits at the same visual height/grid-snap as every other 2x2 building
      rather than floating or sinking relative to them.
    - Verified headless via a real Base scene, not mocked: instantiated a
      live Mill from `BuildingCatalog.get_option("mill")`, confirmed its
      scene resolves to `Workshop.tscn` (not `CropStation.tscn`), confirmed
      `mill is Workshop` and `not (mill is Farm)`, confirmed it's absent
      from `farm_family_options()`, confirmed `has_signal("clicked")` is
      false, confirmed `get_skill_id()`/`get_input_resource()`/
      `resource_type` all read "milling"/"grain"/"flour" correctly after
      catalog application, and confirmed its sprite resolved to the new
      `iso_workstation_workshop.svg` (not falling back to a stale default).
      Then ran a real 40-second production test: `Base._run_job_assignment`
      correctly auto-staffed the Mill with no special-casing, and grain
      100->18 correctly converted into flour 0->12 over that window with
      both buffers left in a sane non-negative state. Not verified
      in-editor for visual quality (whether the new workshop silhouette
      actually reads well at in-game scale/zoom, whether the three tints
      remain distinguishable against it) - recommend a playtest.

- [MOVED] Introduce day / night cycle (Not Ready, Don't Implement Yet)
  - Migrated to Outpost_Survival/Ideas/Day-Night Cycle.md - see below.

---

SUPERSEDED as of 2026-07-15: this file is now a frozen historical log of everything
implemented up through the item above. New work items are no longer queued here -
they go in the Outpost_Survival/ Obsidian vault's Ideas/ folder instead, and the
user tells Claude which Ideas items to implement. Completion write-ups for
newly-implemented ideas go back into that idea's own vault note rather than being
appended to this file. See CLAUDE.md's "Design vault (Obsidian)" section.
