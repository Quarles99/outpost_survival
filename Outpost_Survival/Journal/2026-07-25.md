# 2026-07-25

Executed a prepared session-handoff plan covering a checkpoint commit plus five independent follow-on items.

**Checkpoint.** ~1,400 uncommitted lines (demolish mode, orc raiders/[[Village Raids]], mineable rocks, dirt paths, combat polish, vault updates) went into one commit. Added `Export/` (362 MB build output) to `.gitignore` first. Asked before restoring an `[audio]` bus block the working tree had accidentally dropped from `project.godot` - confirmed accidental, restored it.

Unified the tooltip hover delay (`TooltipManager` was 2.0s, the native `Control.tooltip_text` path was the engine default 0.5s) down to 0.5s everywhere, and fixed a real bug found in the process: `hud.gd`'s `_wire_tooltip()` never set `mouse_filter`, so the five HUD stat tooltips (Water/Population/Idle/Happiness/Day-Night) had never been able to fire at all.

Fixed the "villagers getting stuck delivering wood to farms" bug - `c1fc7cf`'s citizen-vs-citizen RVO avoidance (18px radius, ~36px enforced separation) made `is_navigation_finished()`'s default 10px arrival tolerance unreachable whenever a hauler's target (a WorkSpot) was already occupied by the assigned worker, so the hauler cycled a 60s hang-hatch forever. Added an explicit `ARRIVE_TOLERANCE` (44.0) to `_move_to`'s loop-exit condition, zeroed nav agent velocity on every exit path, set the previously-unset `nav_agent.max_speed` (which had been silently capping the dirt-path speed bonus and speed-skill multiplier), and made `_move_to` return arrival-vs-timeout - groundwork for [[Stuck-worker failsafe]], not yet consumed by any caller.

**Rock sprites - the plan's stated fact turned out false.** The handoff doc named a specific sprite region as an unclaimed "ochre-patched boulder," but it turned out to be the exact same crop `StoneMine.tscn` already uses for its own building - using it for the mineable Rock would have made every rock look like a StoneMine fragment. Found genuine unclaimed boulder sprites in the same sheet via a connected-component scan and a visual comparison artifact; user picked one. Rescaled the decorative pebble to match pixel density.

**Performance pass (partial, left open in [[Performance pass]]).** The `export_presets.cfg` filter was bundling the entire 1.6 GB `sound/` tree into every Web build (33 files actually used out of 4,229). `include_filter` turned out **not** to override `exclude_filter` for ordinary resource files - tested empirically via a real `--export-release` run, not just reasoned about - so the fix moved the 29 actually-`preload()`-ed combat/gather SFX into a new `sound/sfx/` folder and repointed the two source packs' exclusion at that. Verified: 362 MB → 40 MB, all 29 files present. Also deleted `Workstation.tscn`, 16 dead `iso_*.svg`s, 3 dead pack sheets, and 7 dead UIBundleFree sheets plus `__MACOSX` junk. Explicitly left the `combat_unit.gd` O(n²) per-frame cost and `iso_ground.gd`'s per-tile Sprite2D/AtlasTexture allocation for a dedicated future session.

Wrote an honest architectural-impact assessment into [[Change game vision towards dwarf fortress like]] (not restructuring the user's in-progress note) - vertical z-levels is the one that actually invalidates load-bearing systems (`IsoUtils`, `WorldGrid`, the `z_index` draw scheme, 2D navmesh baking); the rest is comparatively additive.

Did a `Docs/` accuracy pass while here and found pre-existing drift unrelated to this session's changes: `stats.md` still said move speed was 70 px/s (code/`Balance.md` both say 100, changed in an earlier session), tree wood-per-tree said 20.0 (actual 80.0), and sapling grow time said 25s (actual 700s, deliberately raised a while back). Fixed all three. Refreshed `roadmap.md`'s map-size entry (56×56, not 28×28) and added missing Implemented entries for demolish mode, mineable rocks, and RVO/dirt-path avoidance. Fixed four confirmed-stale spots in `CLAUDE.md` (`PerimeterWall` fully gone now, `ConstructionSite` is three footprint scenes not one, the `iso_*.svg` set is now entirely pack-cropped art instead, `RESOURCE_VISUALS` deleted) plus two more the SVG deletions caused directly (Workshop's and ConstructionSite's art-file citations, which were already stale before the deletion but only became obviously broken once the cited files stopped existing).

**Open question, unresolved on purpose:** [[Stuck-worker failsafe]]'s "what counts as progress per loop" question - still not answered, no generic stuck-time accumulator was built.

**Verification gap to flag:** interactive in-game testing (hovering tooltips, watching a hauler deliver to an occupied WorkSpot, screenshotting rock sizes) was not done this session - the game window came up on the user's live desktop alongside other running applications, and the user opted to verify manually rather than have this session drive simulated mouse/keyboard input there. Everything above was verified via headless parse checks, a real `--export-release` run for the export-filter fix, and direct code/asset inspection, not by playing the game.
