# 2026-07-15

Set up this Obsidian vault (`Outpost_Survival/`) as a design journal + idea-linking space, separate from `docs/` at the project root (which stays the authoritative gameplay/mechanics reference). Claude now reads and writes here across sessions — see [[Home]].

`Ideas/` also became the implementation backlog today, replacing `Implement_Next.txt` (now frozen/historical) — see `CLAUDE.md`'s "Design vault (Obsidian)" section.

Built a standalone combat-mechanics sandbox (`scenes/combat/CombatTest.tscn`, run directly via F6, or via the Main Menu's "Battle Test" button) per [[Combat System]] and [[Combat Units]] — two auto-fighting teams, no player control. Not wired into the main game; see those notes for the prototype write-up.

Spent most of the session chasing a reported "Swordsmen run to the edge of the map" bug through several wrong diagnoses (RVO avoidance tuning, a hand-rolled kiting-direction blend, a hard spawn-point leash that caused actual deadlocks) before landing on a `Formation` entity with a weighted (not hard) leash - see [[Combat System]]'s dated changelog for the full debugging trail, including two more failed anchor-movement designs after the `Formation` idea itself was agreed on. The bigger vision the user described mid-session (a macro-level strategist AI dynamically choosing formations/tactics) is captured in [[Formation Strategy AI]], deliberately not built this pass.

Vault grew two new top-level sections: [[Planning]] (active work-in-progress tracking, separate from Journal/Ideas) and `Ideas/Completed/` (finished idea notes move here so `Ideas/` stays a clean backlog). The whole `docs/` folder also moved into the vault today, as `Outpost_Survival/Docs/` (via `git mv`, preserving history) - the vault is now this project's entire knowledge base rather than just journal/ideas alongside a separate docs folder. See `CLAUDE.md`'s "Design vault (Obsidian)" section for the updated structure.
